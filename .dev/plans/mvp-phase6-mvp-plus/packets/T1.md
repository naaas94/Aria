# Executor packet — T1: DTO contracts
# Plan: mvp-phase6-mvp-plus · Subtask T1

---

## §1 Task statement (verbatim)

Phase 6 (MVP+) wires the MCP adapter and scratch orchestration engine into the production CLI and API query paths behind an opt-in `--orchestrated` / `orchestrated: true` flag, adds per-step execution trace persistence to the telemetry store beyond the existing aggregate row, and produces a demo shell script and optional asciicast suitable for a portfolio narrative.

Concretely: (1) `MCPToolPortsAdapter` is instantiated with live Neo4j/Chroma clients via a new `build_mcp_adapter(conns)` factory in `aria/services/orchestrated_query.py`; the `index_vectors` stub is replaced with real Chroma indexing; (2) `aria query --orchestrated` and `POST /query` with `orchestrated: true` body field route through `OrchestrationGraph.execute` instead of `run_compliance_query`; the response includes `execution_trace` from `ExecutionResult.to_trace_dict()`; (3) each graph step writes an `agent_executions` row with `agent_name = "orchestration.scratch/{node_name}"`; (4) a demo shell script documents the full orchestrated path for portfolio presentation.

**Non-goals:** Semantic parity with GraphRAG answers; HTTP ingest via orchestrated graph; LangGraph parity; A2A cross-process mesh; Phase 5 doc items; Grafana dashboards.

---

## §2 Shared contracts (verbatim)

### Types / interfaces (T1 scope)

| Symbol | Typed surface | Compat note |
|--------|---------------|-------------|
| `ComplianceQueryRequest.orchestrated: bool = False` | `aria/services/compliance_query.py:ComplianceQueryRequest` | `extra="forbid"` preserved; new field is backward compatible |
| `ComplianceQueryResponse.execution_trace: dict[str, Any] \| None = None` | `aria/services/compliance_query.py:ComplianceQueryResponse` | Default None; key included in `_success_payload` only when not None |

### Error envelope (for context; not owned by T1)

Placeholder gate: CLI exit 1; API HTTP 400 — handled by T4.

### Naming

No new symbols introduced by T1. Decision log for architectural subtask T2 (not T1): `.dev/decision-logs/T2-mcp-adapter-wiring.md`.

### Tests (T1 policy)

No new pytest tests in T1. T1 owns keeping `pytest tests/unit -q` green after the DTO edit. T4 writes the test that exercises the new fields end-to-end.

### CLI surface (for context)

`aria query --orchestrated` — frozen definition owned by T4. T1 does not touch CLI code.

---

## §4 T1 subtask spec (verbatim)

| Field | Content |
|-------|---------|
| **ID** | T1 |
| **Scope** | Add `orchestrated: bool = False` to `ComplianceQueryRequest`; add `execution_trace: dict[str, Any] \| None = None` to `ComplianceQueryResponse`. Both fields use defaults; both are backward compatible. No routing logic in this subtask. |
| **Files to touch** | `aria/services/compliance_query.py` (both Pydantic models, currently lines 26–50) |
| **Contract bindings** | §2 Types (both new fields, compat note); §2 Tests (no new test in T1; T4 owns the smoke test) |
| **Inputs** | None |
| **Outputs** | Updated `aria/services/compliance_query.py`. `pytest tests/unit -q` remains green after the edit. |
| **Kill criteria** | 1. Halt if `ComplianceQueryRequest.model_config` does not have `extra="forbid"` after the edit — must be preserved. 2. Halt if `pytest tests/unit -q` shows any new failures after adding the fields — likely cause is exact-key-equality assertion on `_success_payload`; report the failing test name and await instruction before proceeding. 3. Halt if Phase 1–4 plan files do not all show `Status: Complete` at execution start (prerequisite gate). Read `.dev/plans/mvp-phase1-golden-wet-run/plan.md`, `phase-2-eval-honesty/plan.md`, `phase-3-observability/plan.md`, `mvp-phase4-product-defaults-ux/plan.md` and verify each has `Status: Complete`. Report any non-Complete status. |
| **Log tier** | `standard` |
| **Risks & mitigations** | *Risk:* Phase 4 test `test_query_json_placeholder_returns_valid_payload` uses exact set equality on `_success_payload` keys. `execution_trace` is included in `_success_payload` only when not None — placeholder path leaves `None`, key omitted. Kill criterion 2 catches any regression. *Risk:* `Any` import for `dict[str, Any]` — verify `from typing import Any` already present in `aria/services/compliance_query.py` (line 6: `from typing import Any, Literal, Protocol` — confirmed at plan time). |

---

## §5.2 Load-bearing assumptions (filtered for T1)

1. `ComplianceQueryRequest.orchestrated: bool = False is backward compatible with extra="forbid" | aria/services/compliance_query.py:ComplianceQueryRequest ↔ api/routers/query.py ↔ tests/eval/test_api_contracts.py | if adding a field breaks existing API tests (e.g. exact model validation), kill T1-KC2 fires | T1`

2. `Phases 1–4 plan files all have Status: Complete at T1 execution time | .dev/MVP_PICKUP.md Phase 6 gate ↔ T1 kill criterion 3 | executing MVP+ on incomplete foundation | T1`

3. `_success_payload includes execution_trace only when not None | aria/cli/commands/query.py:_success_payload ↔ tests/unit/test_cli_entry.py:test_query_json_placeholder_returns_valid_payload | key conditionally included; Phase 4 test unaffected | T1, T4` *(Note: T4 owns the _success_payload edit; T1 must not touch query.py.)*

---

## §5.4 Hidden couplings (filtered for T1)

1. `Phase 4 test exact key equality on _success_payload | tests/unit/test_cli_entry.py:test_query_json_placeholder_returns_valid_payload ↔ aria/cli/commands/query.py:_success_payload | if the test asserts exactly 5 keys and execution_trace is always added as null, test fails; risk is low because _success_payload edit is owned by T4 (not T1) — T1 only adds the field to ComplianceQueryResponse with default None | suspected | T1, T4`

---

## Resolved inputs

No prior subtask inputs required. T1 executes first.

**Prerequisite check (do this first):** Read the four Phase 1–4 plan status headers before making any edits:
- `.dev/plans/mvp-phase1-golden-wet-run/plan.md` → expected: `Status: Complete`
- `.dev/plans/phase-2-eval-honesty/plan.md` → expected: `Status: Complete`
- `.dev/plans/phase-3-observability/plan.md` → expected: `Status: Complete`
- `.dev/plans/mvp-phase4-product-defaults-ux/plan.md` → expected: `Status: Complete`

If any shows a different status, halt and report before proceeding.
