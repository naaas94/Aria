# Executor packet — T4: CLI + API orchestrated routing
# Plan: mvp-phase6-mvp-plus · Subtask T4

---

## §1 Task statement (verbatim)

Phase 6 (MVP+) wires the MCP adapter and scratch orchestration engine into the production CLI and API query paths behind an opt-in `--orchestrated` / `orchestrated: true` flag, adds per-step execution trace persistence to the telemetry store beyond the existing aggregate row, and produces a demo shell script and optional asciicast suitable for a portfolio narrative.

T4 specifically delivers item (2): `aria query --orchestrated` (CLI) and `POST /query` with `orchestrated: true` body field (API) route through `OrchestrationGraph.execute` instead of `run_compliance_query`; the response includes `execution_trace` from `ExecutionResult.to_trace_dict()`.

**Non-goals:** No changes to the standard (non-orchestrated) query path. No trajectory eval path changes. No new routes.

---

## §2 Shared contracts (verbatim)

### Types / interfaces (T4 scope — consuming from T1 and T2)

| Symbol | Source | T4 consumes |
|--------|--------|-------------|
| `ComplianceQueryRequest.orchestrated: bool = False` | T1 output (`aria/services/compliance_query.py`) | CLI passes `req.orchestrated = as_orchestrated`; API reads `request.orchestrated` |
| `ComplianceQueryResponse.execution_trace: dict[str, Any] \| None = None` | T1 output | Included in `_success_payload` only when not None |
| `run_orchestrated_query(request_dto, conns) -> ComplianceQueryOutcome` | T2 output (`aria/services/orchestrated_query.py`) | Both CLI and API call this when `orchestrated=True` |

### Error envelope (frozen — T4 implements these exactly)

| Trigger | Shape |
|---------|-------|
| `--orchestrated` + `ARIA_PLACEHOLDER_API=true` (CLI) | Exit 1; `print("Orchestrated mode requires live backends. Set ARIA_PLACEHOLDER_API=false.", file=sys.stderr)` |
| `orchestrated=true` + `placeholder_api_enabled()=true` (API) | HTTP 400; `{"detail": "Orchestrated mode requires live backends. Set ARIA_PLACEHOLDER_API=false."}` |
| `run_orchestrated_query` returns `ComplianceQueryUnavailable` | CLI: existing unavailable handling (exit 1, detail message); API: existing 503 `ServiceUnavailableBody` pattern |

### Naming (frozen — T4 owns)

| Symbol / path | Definition |
|---------------|-----------|
| `aria query --orchestrated` | `as_orchestrated: Annotated[bool, typer.Option("--orchestrated", help="Route query through OrchestrationGraph (vector-only; exposes agent trace). Requires live backends.")] = False` |
| `X-ARIA-Mode: orchestrated-live` | API response header value for orchestrated + live path only. Non-orchestrated paths keep existing `"live"` / `"placeholder"` values. |
| `test_orchestrated_cli_smoke_placeholder_blocked` | In `tests/unit/test_cli_entry.py` — CliRunner test |

### Tests (T4 policy)

`test_orchestrated_cli_smoke_placeholder_blocked`: 
```python
result = runner.invoke(app, ["query", "test question", "--orchestrated"],
                       env={"ARIA_PLACEHOLDER_API": "true"})
assert result.exit_code == 1
assert "Orchestrated mode requires live backends" in result.output or result.stderr
```

Must NOT break: `test_query_json_placeholder_returns_valid_payload` (Phase 4). The `_success_payload` update includes `execution_trace` **only when not None** — non-orchestrated placeholder path keeps `execution_trace=None` (default from T1), so the key is absent from the payload. Phase 4 test is unaffected.

### CLI surface (frozen by T4)

| Flag | Frozen definition |
|------|------------------|
| `aria query --orchestrated` | Typer option `"--orchestrated"`, default `False`, `bool` type |
| `aria query --json` | Pre-existing; JSON output gains `execution_trace` key when orchestrated + live |

---

## §4 T4 subtask spec (verbatim)

| Field | Content |
|-------|---------|
| **ID** | T4 |
| **Scope** | (a) **CLI:** Add `--orchestrated/--no-orchestrated` flag to `aria query`. In `_query_async`: if `as_orchestrated=True` and `use_placeholder=True`, print error to stderr and return exit code 1. If `as_orchestrated=True` and live, call `run_orchestrated_query(req, conns)` instead of `run_compliance_query`. Include `execution_trace` in `_success_payload` when not None. (b) **API:** In `api/routers/query.py`: if `request.orchestrated=True` and `placeholder_api_enabled()=True`, return HTTP 400 with error body. If `request.orchestrated=True` and live, call `run_orchestrated_query`; set `response.headers["X-ARIA-Mode"] = "orchestrated-live"`. (c) **Test:** Add `test_orchestrated_cli_smoke_placeholder_blocked` to `tests/unit/test_cli_entry.py`. |
| **Files to touch** | `aria/cli/commands/query.py`, `api/routers/query.py`, `tests/unit/test_cli_entry.py` |
| **Contract bindings** | §2 CLI surface (`--orchestrated` flag string, frozen); §2 Error envelope (placeholder gate messages, frozen); §2 Naming (`X-ARIA-Mode: orchestrated-live`, test function name); §2 Types (all symbols above) |
| **Inputs** | T2 (`run_orchestrated_query` importable from `aria/services/orchestrated_query.py`) |
| **Outputs** | Updated `aria/cli/commands/query.py`; updated `api/routers/query.py`; updated `tests/unit/test_cli_entry.py`; `pytest tests/unit -q` green. |
| **Kill criteria** | 1. Halt if `from aria.services.orchestrated_query import run_orchestrated_query` raises `ImportError` — T2 not complete or circular import; report and await. 2. Halt if `test_orchestrated_cli_smoke_placeholder_blocked` does not pass (exit code must be 1; stderr/output must contain the placeholder-blocked message). 3. Halt if existing Phase 4 test `test_query_json_placeholder_returns_valid_payload` fails — `_success_payload` for non-orchestrated path must be unmodified (no `execution_trace` key for `None` case). 4. Halt if `tests/eval/test_trajectory_eval.py` fails after API routing change — routing must not alter `EDGE_MAP` or `CANONICAL_SCRATCH_*` path constants (T4 only adds a branch before calling the graph; it does not touch edges.py or paths.py). 5. Halt if `pytest tests/unit -q` has new failures beyond the newly-added test. |
| **Log tier** | `standard` |
| **Risks & mitigations** | *Risk (Surface 4/confirmed):* `ARIAState` routing depends on `regulation_id` vs `query` fields. CLI must pass `ARIAState(query=request_dto.question, regulation_id=request_dto.regulation_id)` — verify exact `ARIAState` field names from `aria/orchestration/scratch/state.py` before using (handled by T2's implementation of `run_orchestrated_query`; T4 just calls the function). *Risk (§5.4 #3/suspected):* Existing API contract tests may assert `X-ARIA-Mode` header value — read `tests/eval/test_api_contracts.py` before editing `api/routers/query.py` to check if non-orchestrated header assertions would conflict. *Risk:* `_success_payload` conditional `execution_trace` inclusion — use `if outcome.response.execution_trace is not None: payload["execution_trace"] = ...` pattern; do not always include. |

---

## §5.2 Load-bearing assumptions (filtered for T4)

1. `AppConnections provides non-None neo4j and vector_store via connect_app_dependencies(strict=True) | api/connections.py:connect_app_dependencies ↔ run_orchestrated_query called from CLI/API | MCPServer initialized with None clients → RuntimeError at first node; kill T4-KC1 would fire if T2 not complete | T2, T4`

2. `_success_payload includes execution_trace only when not None | aria/cli/commands/query.py:_success_payload ↔ tests/unit/test_cli_entry.py:test_query_json_placeholder_returns_valid_payload | Phase 4 test unaffected; key conditionally included | T1, T4`

---

## §5.4 Hidden couplings (filtered for T4)

1. `Trajectory eval path constants vs orchestrated routing | aria/orchestration/scratch/paths.py:CANONICAL_SCRATCH_* ↔ edges.EDGE_MAP ↔ tests/eval/test_trajectory_eval.py | T4 adds a branch that calls graph.execute but does not modify EDGE_MAP or path constants; trajectory tests safe | confirmed | T4`

2. `X-ARIA-Mode header in API contract tests | api/routers/query.py:response.headers["X-ARIA-Mode"] ↔ tests/eval/test_api_contracts.py | "orchestrated-live" only set when request.orchestrated=True; existing tests use non-orchestrated requests; only fails if tests assert header is from a closed set | suspected | T4`

3. `placeholder gate behavior undefined before T4 | api/config.py:placeholder_api_enabled ↔ cli/commands/query.py ↔ api/routers/query.py | without explicit gate, placeholder + orchestrated yields RuntimeError or 503 instead of clear 400/exit-1 | confirmed | T4`

4. `Phase 4 test exact key equality on _success_payload | tests/unit/test_cli_entry.py:test_query_json_placeholder_returns_valid_payload ↔ _success_payload | conditional inclusion (not None) means Phase 4 test is unaffected; risk if test uses superset check that fails on extra key | suspected | T1, T4`

---

## Resolved inputs

**T2 must complete before T4 executes.** Verify:
- `aria/services/orchestrated_query.py` exists and exports `run_orchestrated_query`
- `pytest tests/unit -q` is green at T2 HEAD

**Key code locations to read before editing:**
- `aria/cli/commands/query.py:57–103` — `_query_async` function where the new `as_orchestrated` branch goes
- `aria/cli/commands/query.py:47–54` — `_success_payload` where `execution_trace` conditional inclusion goes
- `api/routers/query.py` — read the full file to understand existing placeholder gate and `X-ARIA-Mode` header setting
- `tests/eval/test_api_contracts.py` — scan for `X-ARIA-Mode` assertions before editing the router
- `tests/unit/test_cli_entry.py` — re-read before editing; ensure `test_query_json_placeholder_returns_valid_payload` is intact
