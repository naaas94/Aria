# Executor packet — T5: Status/preflight clarification message

**Plan:** mvp-phase1-golden-wet-run v1.0  
**Executor skill:** executor-subtask-execution (read that skill before starting)  
**Parallel group:** {T1, T2, T3, T4, T5} — may run in parallel; T6 depends on this task completing.

---

## §1 Task statement (verbatim)

Phase 1 of the Aria MVP is a one-session golden-path wet run that proves the end-to-end live stack operates correctly. The session exercises the full Phase 1 command sequence — `docker compose up -d neo4j chromadb`, `ARIA_PLACEHOLDER_API=false`, then `aria init` → `aria ingest <sample>` → `aria query` → `aria impact <id>` → `aria telemetry` → optional `aria serve` + curl → `pytest tests/integration -m integration` — records every failure inline in `.dev/MVP_PICKUP.md`'s wet run log, applies minimal targeted fixes for each blocker, and concludes with a signed-off confirmation that `aria ingest` preflight (Neo4j + Chroma + LLM all required) is strictly more demanding than `/ready` (LLM optional).

**Non-goals:** Phases 2–6; HTTP-level ingest; comprehensive CliRunner test suites; refactoring beyond minimal Phase 1 blockers; production hardening; scratch orchestration layer changes.

---

## §2 Shared contracts (verbatim)

**Decision log path (T6, architectural):** `.dev/decision-logs/T6-wet-run.md`

### Logging (binding)

- `aria status` output: adds a visible note that LLM health is **optional** for `aria status` exit code (exits 0 even if LLM fails), but is **required** for `aria ingest`. T5 owns.
- No new structured telemetry fields.

### Types / interfaces

| Surface | Owner | Typed path | Round-trip / construction test |
|---------|-------|-----------|-------------------------------|
| `aria status` note re: LLM requirement | **T5** | `aria/cli/commands/status.py` — note appended to non-JSON output | Visual inspection in T6 |
| `tests/fixtures/sample_regulation.html` | T1 | Committed HTML file | T6 |
| `serve` `--port` default | T3 | `aria/cli/commands/serve.py` | T3 |
| `@pytest.mark.integration` on `tests/integration/` | T2 | Both test files + `pyproject.toml` | T6 |
| `aria ingest` stdout — regulation ID line | T4 | `aria/cli/commands/ingest.py` | T6 |

---

## §4 Subtask spec — T5

**Scope:** Add a visible clarifying note to `aria status` human-readable output indicating that LLM health is optional for `aria status` (can exit 0 with LLM unhealthy) but required for `aria ingest`. Output-only change; no logic or exit-code modification. Resolves Flag 5.

**Files to touch:**
- `aria/cli/commands/status.py`

**Contract bindings:** Logging: status output message extended with one note line. No CLI surface change (command signature unchanged, exit codes unchanged).

**Inputs:** None.

**Outputs:**
- Modified `aria/cli/commands/status.py` with a footer note in the non-JSON output path. Suggested text: `"Note: aria ingest additionally requires LLM. aria status exits 0 even when LLM is unavailable."` (or equivalent phrasing that clearly conveys the asymmetry).
- Changelog entry.

**Kill criteria:**
- Halt if `aria status` emits JSON-only output unconditionally (no human-readable path exists). In that case, add the note only to the non-`--json` code path; if only JSON is produced, add an `"ingest_note"` key to the JSON output describing the LLM asymmetry.

**Log tier:** trivial

**Risks & mitigations:**
- None material. This is a display-only change. Verify the note appears correctly by reading the status output structure in `aria/cli/commands/status.py` before editing.

---

## Filtered §5.2 — Load-bearing assumptions (T5)

No §5.2 items are specific to T5 alone. This task has no load-bearing assumptions; it is a display-only addition.

---

## Filtered §5.4 — Hidden couplings (T5)

No §5.4 items are specific to T5 alone. No cross-subtask coupling for a display-only change.

---

## Resolved inputs

None. T5 has no dependency on prior subtask outputs.
