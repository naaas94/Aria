# Executor packet — T8: Plan closure and §8 auditor handoff

**Plan:** mvp-phase1-golden-wet-run v1.1 (amendment)  
**Executor skill:** executor-subtask-execution (read that skill before starting)  
**Sequential:** Requires T7 complete.

---

## §1 Task statement (verbatim)

Phase 1 of the Aria MVP is a one-session golden-path wet run that proves the end-to-end live stack operates correctly. The session exercises the full Phase 1 command sequence — `docker compose up -d neo4j chromadb`, `ARIA_PLACEHOLDER_API=false`, then `aria init` → `aria ingest <sample>` → `aria query` → `aria impact <id>` → `aria telemetry` → optional `aria serve` + curl → `pytest tests/integration -m integration` — records every failure inline in `.dev/MVP_PICKUP.md`'s wet run log, applies minimal targeted fixes for each blocker, and concludes with a signed-off confirmation that `aria ingest` preflight (Neo4j + Chroma + LLM all required) is strictly more demanding than `/ready` (LLM optional).

**Non-goals:** Phases 2–6; HTTP ingest; comprehensive CliRunner live-path tests; refactoring beyond minimal Phase 1 blockers; production hardening; scratch orchestration changes.

**Amendment non-goals (T8):** Implement F-08 wet-run replay automation. Re-open T1–T6 code scope.

---

## §2 Shared contracts — amendment deltas (verbatim from T7)

### Tests (*Landed:*)

| Surface | Owner | Path | Verification |
|---------|-------|------|--------------|
| `aria serve` port default + `API_PORT` | T3 | `tests/unit/test_serve.py` | `pytest tests/unit/test_serve.py` — 4 tests |
| `aria ingest` regulation ID stdout + `_fetch_regulation_ids` | T4 | `tests/unit/test_ingest_command.py` | `pytest tests/unit/test_ingest_command.py` — 5 tests |
| `aria status` ingest/LLM note | T5 | `tests/unit/test_status.py` | `pytest tests/unit/test_status.py` — 2 tests |

### Logging (*Landed:*)

| Surface | Owner | Binding literal |
|---------|-------|-----------------|
| `aria status` footer note | T5 | `"Note: aria ingest additionally requires LLM. aria status exits 0 even when LLM is unavailable."` |
| `aria ingest` regulation_ids (none branch) | T4 | `"  regulation_ids: (none — Regulation nodes not found; use seed_graph.py IDs or check entity extractor)"` |

### Verification commands (§8.1)

Run on **clean checkout** of closure SHA (not a dirty working tree):

```bash
pytest tests/integration -m integration -q
pytest tests/unit/test_serve.py tests/unit/test_ingest_command.py tests/unit/test_status.py -q
```

Audit baseline at `d448a310`: 25 integration passed, 11 unit passed.

---

## §4 Subtask spec — T8

| Field | Content |
|-------|---------|
| **ID** | T8 |
| **Scope** | Mark plan **Complete**; populate §8 auditor handoff at closure SHA. Refresh context map at HEAD (F-01). Commit audit artifact if untracked. |
| **Files to touch** | `.dev/plans/mvp-phase1-golden-wet-run/plan.md`, `.dev/plans/mvp-phase1-golden-wet-run/context-map.md`, `.dev/audits/2026-05-30-mvp-phase1-golden-wet-run.md` |
| **Contract bindings** | §8 schema (§8.1–§8.6). Context map SHA = closure SHA. |
| **Inputs** | T7 (§2 Amendment block + CHANGELOG landed) |
| **Outputs** | Plan Status **Complete**; §8 populated; context map re-scouted; audit committed |
| **Kill criteria** | (a) Halt if T7 §2 Amendment block missing. (b) Halt if §8.1 run on dirty tree. (c) Halt if any §8.2 path fails `git show HEAD:<path>`. (d) Halt if context map lacks post-execution staleness note when SHA ≠ scout SHA. |
| **Log tier** | standard |
| **Risks & mitigations** | Record actual closure SHA in §8.1, not audit SHA `d448a310`, if T7 commits exist. |

---

## §8 template — populate in plan (replace deferred stub)

### §8.1 Completion snapshot

- **Tree SHA:** `<closure SHA>`
- **Verification (clean checkout):**
  - `pytest tests/integration -m integration -q` → `<N> passed`
  - `pytest tests/unit/test_serve.py tests/unit/test_ingest_command.py tests/unit/test_status.py -q` → `<N> passed`
- **Exit code:** 0

### §8.2 Artifact chain

| Order | Path |
|-------|------|
| 1 | `.dev/plans/mvp-phase1-golden-wet-run/context-map.md` (note scout SHA `ee870022` vs closure SHA) |
| 2 | `.dev/plans/mvp-phase1-golden-wet-run/plan.md` (v1.1) |
| 3 | `.dev/plans/mvp-phase1-golden-wet-run/packets/T{1..8}.md` |
| 4 | `.dev/decision-logs/T6-wet-run.md` |
| 5 | `.dev/audits/2026-05-30-mvp-phase1-golden-wet-run.md` |
| 6 | `.dev/MVP_PICKUP.md` (wet run log L249–316) |
| 7 | `CHANGELOG.md` § mvp-phase1-golden-wet-run |

### §8.3 §2 evidence (per-row)

| §2 row | Landed artifact | Test / check |
|--------|-----------------|--------------|
| sample_regulation.html | `tests/fixtures/sample_regulation.html` | T6 wet run ingest exit 0 |
| integration marker | `tests/integration/test_*.py`, `pyproject.toml` | `pytest tests/integration -m integration` 25 passed |
| serve --port default | `aria/cli/commands/serve.py` | `tests/unit/test_serve.py` |
| regulation_ids stdout | `aria/cli/commands/ingest.py` | `tests/unit/test_ingest_command.py` |
| status LLM note | `aria/cli/commands/status.py` | `tests/unit/test_status.py` |

*Include §2 Amendment rows with same evidence discipline.*

### §8.4 §5 disposition

| Item | Status | Notes |
|------|--------|-------|
| §5.2 integration marker CI exclusion | **closed** | T2 changelog CI note; 25 tests pass |
| §5.2 html_parser generic HTML | **closed** | T1 sample + T6 ingest |
| §5.2 Neo4jClient.execute_read | **closed** | `test_fetch_regulation_ids_*` |
| §5.2 entity extractor Regulation.id | **closed** | Wet run printed `reg-gdpr`; audit ruled out key mismatch |
| §5.2 LLM reachable during wet run | **closed** | OpenAI fallback documented in T6 log |
| §5.2 Typer serve port env default | **closed** | Pattern B + `test_serve.py` |
| §5.4 Chroma/serve port collision | **closed** | T3 default 8080 |
| §5.4 pytest not integration CI | **closed** | Changelog flagged |
| §5.4 Regulation id key suspected | **closed** | Audit disproven |
| §5.4 seed_corpus bypass | **closed** | T6 avoided |
| §5.4 scratch import collection | **closed** | 25 collected |
| F-01 context map stale | **closed** | T8 context-map refresh |
| F-08 wet-run replay | **open** | Deferred non-goal; non-blocking |

### §8.5 Cold-read seeds

1. `aria/cli/commands/ingest.py`
2. `aria/cli/commands/serve.py`
3. `aria/cli/commands/status.py`
4. `tests/unit/test_ingest_command.py`
5. `.dev/MVP_PICKUP.md` (wet run log)
6. `.dev/decision-logs/T6-wet-run.md`

### §8.6 Audit remediation cross-link

| Audit finding | Remediation | §2 *Landed:* |
|---------------|-------------|--------------|
| F-02, F-03 | T7 — unit test files in §2 Amendment Tests | Tests table |
| F-04, F-05, F-06 | T7 — stdout literals + ingest test row | Logging + Types tables |
| F-07 | T8 — §8 populated, Status Complete | §8.1–§8.6 |
| F-01 | T8 — context map refresh | §8.2 item 1 staleness note |

---

## Filtered §5.2 — Load-bearing assumptions (T8)

```
(Shipped stdout literals in status.py and ingest.py match audit evidence at HEAD |
 §2 Amendment Logging rows |
 T7 lands incorrect binding literals; re-audit fails on F-04/F-05 | T7, T8)
```

---

## Filtered §5.4 — Hidden couplings (T8)

```
confirmed |
(context-map.md SHA recorded at §0 diverges from closure SHA |
 §8.2 artifact chain; §0 context map intake |
 auditor treats scout predictions as authoritative without staleness note | T8)
```

---

## Resolved inputs

| Input | Status |
|-------|--------|
| T7 | Must be complete — §2 Amendment block in plan; CHANGELOG updated for T3/T5 tests |
| Audit | `.dev/audits/2026-05-30-mvp-phase1-golden-wet-run.md` |

**Context map refresh (F-01):** Update scout SHA, HEAD SHA, and note that §File map paths reflect post-T1–T6 state. Do not rewrite historical Flag IDs; add a *Post-execution* section.
