# Executor packet — T7: Audit contract reconciliation

**Plan:** mvp-phase1-golden-wet-run v1.1 (amendment)  
**Executor skill:** executor-subtask-execution (read that skill before starting)  
**Sequential:** T7 must complete before T8 starts. T6 is complete.

---

## §1 Task statement (verbatim)

Phase 1 of the Aria MVP is a one-session golden-path wet run that proves the end-to-end live stack operates correctly. The session exercises the full Phase 1 command sequence — `docker compose up -d neo4j chromadb`, `ARIA_PLACEHOLDER_API=false`, then `aria init` → `aria ingest <sample>` → `aria query` → `aria impact <id>` → `aria telemetry` → optional `aria serve` + curl → `pytest tests/integration -m integration` — records every failure inline in `.dev/MVP_PICKUP.md`'s wet run log, applies minimal targeted fixes for each blocker, and concludes with a signed-off confirmation that `aria ingest` preflight (Neo4j + Chroma + LLM all required) is strictly more demanding than `/ready` (LLM optional).

Five pre-run blockers identified by the context map (Flags 1–5) are resolved in dedicated subtasks before the wet run executes (T1–T5). T6 is the execution subtask itself.

**Non-goals:** Phases 2–6 of MVP_PICKUP.md (eval/honesty, observability, product defaults, UX). HTTP-level ingest (`POST /ingest/*`). Comprehensive `CliRunner` live-path test suites (Flag 6 deferred). Refactoring any existing service beyond the minimal change that unblocks Phase 1. Production hardening, load testing, or persistence guarantees. Changing or auditing the scratch orchestration layer (`aria/orchestration/scratch/`).

**Amendment non-goals (T7):** Revert unit tests shipped in T3–T5. Implement wet-run replay automation (F-08). Change `aria serve`/`ingest`/`status` code unless HEAD literals diverge from audit evidence.

---

## §2 Shared contracts — amendment deltas (binding for T7)

*T7 lands the block below into the plan. Do not edit original §2 rows in §1–§6 of the parent plan.*

### Tests (*Landed:* — closes audit F-02, F-03)

| Surface | Owner | Path | Verification |
|---------|-------|------|--------------|
| `aria serve` port default + `API_PORT` | T3 | `tests/unit/test_serve.py` | `pytest tests/unit/test_serve.py` — 4 tests |
| `aria ingest` regulation ID stdout + `_fetch_regulation_ids` | T4 | `tests/unit/test_ingest_command.py` | `pytest tests/unit/test_ingest_command.py` — 5 tests |
| `aria status` ingest/LLM note | T5 | `tests/unit/test_status.py` | `pytest tests/unit/test_status.py` — 2 tests |

*Supersedes original §2 Tests row "No new test files created by T1–T5."*

### Logging (*Landed:* — closes audit F-04, F-05)

| Surface | Owner | Binding literal |
|---------|-------|-----------------|
| `aria status` footer note | T5 | `"Note: aria ingest additionally requires LLM. aria status exits 0 even when LLM is unavailable."` |
| `aria ingest` regulation_ids (none branch) | T4 | `"  regulation_ids: (none — Regulation nodes not found; use seed_graph.py IDs or check entity extractor)"` |
| `aria ingest` regulation_ids (success branch) | T4 | `"  regulation_ids: <comma-separated>"` |

### Types / interfaces (*Landed:* — closes audit F-06)

| Surface | Owner | Typed path | Round-trip / construction test |
|---------|-------|-----------|-------------------------------|
| `aria ingest` stdout — regulation ID line | T4 | `aria/cli/commands/ingest.py` | `tests/unit/test_ingest_command.py` |

### CLI surface (unchanged)

All Phase 1 CLI strings from original §2 remain frozen. T7 is documentation-only unless kill criterion (b) fires.

---

## §4 Subtask spec — T7

| Field | Content |
|-------|---------|
| **ID** | T7 |
| **Scope** | Back-annotate shared contracts with *Landed:* deltas so shipped unit tests and stdout literals are binding without reverting beneficial coverage. Acknowledge T3/T5 test files in CHANGELOG. |
| **Files to touch** | `.dev/plans/mvp-phase1-golden-wet-run/plan.md` (verify §2 Amendment block present — append only if orchestrator draft missing), `CHANGELOG.md` |
| **Contract bindings** | §2 Amendment block above. Tests and Logging tables. |
| **Inputs** | T1–T6 complete. Audit: `.dev/audits/2026-05-30-mvp-phase1-golden-wet-run.md` |
| **Outputs** | (1) Plan contains §2 Amendment — contract deltas block under v1.1 extension. (2) CHANGELOG acknowledges `tests/unit/test_serve.py` (T3) and `tests/unit/test_status.py` (T5). (3) Changelog line for plan v1.1 amendment. |
| **Kill criteria** | (a) Halt if any of the three unit test files are absent at HEAD. (b) Halt if shipped stdout in `status.py` or `ingest.py` differs from audit-cited literals and fix requires > 1 code file. (c) Halt if amendment edits prose in original §1–§6 (only append/verify v1.1 block and CHANGELOG). |
| **Log tier** | standard |
| **Risks & mitigations** | Copy literals from HEAD `aria/cli/commands/status.py` and `ingest.py`, not from original T5 spec quote. |

---

## Filtered §5.2 — Load-bearing assumptions (T7)

```
(Shipped stdout literals in status.py and ingest.py match audit evidence at HEAD |
 §2 Amendment Logging rows |
 T7 lands incorrect binding literals; re-audit fails on F-04/F-05 | T7, T8)
```

---

## Filtered §5.4 — Hidden couplings (T7)

No T7-specific hidden couplings beyond the load-bearing assumption above.

---

## Resolved inputs

| Input | Status |
|-------|--------|
| T6 | Complete — wet run log signed off Y; `.dev/decision-logs/T6-wet-run.md` at HEAD |
| Audit | `.dev/audits/2026-05-30-mvp-phase1-golden-wet-run.md` — findings F-02, F-03, F-04, F-05, F-06 assigned to T7 |

**Verification commands (run before marking complete):**

```bash
pytest tests/unit/test_serve.py tests/unit/test_ingest_command.py tests/unit/test_status.py -q
```

Expected: 11 passed.
