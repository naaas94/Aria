# Executor packet — T6: §2 contract reconciliation

**Plan:** phase-2-eval-honesty v1.2 (amendment)  
**Packet:** T6  
**Executor skill:** executor-subtask-execution (read it before starting)  
**Sequential:** T6 may run in parallel with T5. T7 requires T6 complete.

---

## §1 Task statement (verbatim)

Phase 2 of the MVP eval-honesty pass makes the golden set's five failing medium-tier retrieval cases deterministically pass by filling their empty `retrieved_context` fields with keyword-matching synthetic text (Option A). It also removes a permanently-true sub-check stub for `requires_multi_hop` in the retrieval runner, renames a misleading CI step label ("includes replay" when no replay case exists), and adds one `tier:slow` replay case backed by a committed hand-authored JSON fixture to exercise the existing `run_replay_check` code path end-to-end. The Phase 2 exit criterion is a green `pytest tests/eval/golden_set/test_goldens.py --golden-tier=slow` run.

**Non-goals:**
- No live HybridRetriever calls in CI
- No recording script for EvalRecorder
- No changes to trace, security, contract, or edge case categories
- No changes to `tests/unit/` or `tests/integration/`
- No Phase 3 observability, Phase 4 CLI defaults, or Phase 5 doc hygiene
- No changes to `aria/llm/` or `aria/retrieval/hybrid_retriever.py`
- No changes to `tests/eval/graphrag_vs_vector_rag.py` or `DEFAULT_COMPONENT_KEYWORDS`
- No Option B wiring for medium-tier cases

**Amendment non-goals (T6):** Edit original §§0–6 or §8 prose (except T7 owns §8 updates). Rename shipped tests to match stale §2 literal. Implement F-05/F-06 coverage gaps.

---

## §2 Shared contracts — amendment deltas (binding for T6)

*T6 verifies the v1.2 plan block **§2 Amendment — contract deltas** is present and aligns with HEAD. Do not edit original §2 table rows in §§0–6.*

| Topic | *Landed:* delta |
|-------|-----------------|
| **Tests — replay round-trip symbol** | Shipped name: `test_run_replay_check_passes_with_inline_fixture` in `tests/eval/golden_set/test_runner_unit.py` (supersedes original §2 Typed-surface reference to `test_run_replay_check_with_fixture`). Golden slow tier still provides on-disk `eval-replay-gdpr-erasure.json` E2E round-trip. |
| **Tests — CI gate for runner unit file** | PR CI: `.github/workflows/ci.yml` `Golden set (fast tier)` step runs `pytest tests/eval/golden_set/test_runner_unit.py` (owned by T5). |
| **Tests — deferred (unchanged)** | F-05 committed-fixture unit load without mock; F-06 golden negative for wrong `retrieved_context` — remain deferred per audit §14. |

---

## §4 Subtask spec — T6

**ID:** T6

**Scope:** Ensure plan v1.2 extension contains **§2 Amendment — contract deltas** with correct shipped test name. Update `CHANGELOG.md` with v1.2 amendment note. Update decision log header plan version to v1.2.

**Files to touch:**
- `.dev/plans/phase-2-eval-honesty/plan.md` (verify v1.2 §2 Amendment block only — orchestrator may have pre-drafted; append missing rows only)
- `CHANGELOG.md`
- `.dev/decision-logs/T2-requires-multi-hop.md` (header `Plan:` line only)

**Contract bindings:** Amendment §2 block above.

**Inputs:** T1–T4 complete. Audit: `.dev/audits/2026-05-30-phase-2-eval-honesty.md` (F-04).

**Outputs:** (1) Plan v1.2 §2 Amendment block present and accurate. (2) CHANGELOG entry for v1.2 amendment (T5–T7). (3) Decision log references plan v1.2.

**Kill criteria:**
- (a) HALT if amendment edits prose in original §§0–6 or §8 (T7 owns §8).
- (b) HALT if `test_run_replay_check_passes_with_inline_fixture` is absent at HEAD — report actual function name.
- (c) HALT if reconciliation requires renaming tests instead of plan *Landed:* row (escalate).

**Log tier:** standard

**Risks & mitigations:** Read function name from `tests/eval/golden_set/test_runner_unit.py` at HEAD, not from original §2 Typed-surface bullet.

---

## Filtered §5.2 load-bearing assumptions (T6)

- `(§2 Typed-surface names test_run_replay_check_with_fixture | §2 Tests row ↔ test_runner_unit.py def names | auditor F-04 contract-violation if plan not amended | T6)` — Closes F-04 via *Landed:* row, not test rename.

---

## Resolved inputs

Audit F-04: §2 cited `test_run_replay_check_with_fixture`; shipped `test_run_replay_check_passes_with_inline_fixture`.

## Execution guidance

1. Confirm orchestrator-drafted **§2 Amendment — contract deltas** under plan v1.2 extension; add rows only if missing.
2. CHANGELOG example line:
   - `T6 (Plan v1.2 contract reconciliation): §2 Amendment records shipped replay test name and CI gate for test_runner_unit.py; see audit F-04.`
3. Decision log header: `**Plan:** phase-2-eval-honesty v1.2`
4. Do not touch `runner.py` or test bodies.
