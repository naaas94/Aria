# Executor packet — T2 · README live-mode Quickstart block

**Plan:** mvp-phase4-product-defaults-ux  
**Log tier:** `standard`

---

## §1 Task statement (verbatim)

Phase 4 resolves the long-standing G8 placeholder-default UX gap and improves the operator getting-started experience. Concretely: (1) the code default for `ARIA_PLACEHOLDER_API` is flipped from `true` to `false` so operators who skip the README cannot accidentally believe the system works without backends; (2) the README Quickstart gains an explicit "live mode" block showing `ARIA_PLACEHOLDER_API=false` with the full stack; (3) the wet run log template in `.dev/MVP_PICKUP.md` gains explicit `LLM_MODEL=` and `LLM_BASE_URL=` fields so Phase 1 session data capture is unambiguous; and (4) a `aria query --json` CLI smoke test is added to the unit suite to provide contract coverage for the `--json` code path that currently has zero tests.

**Non-goals:**
- Phase 1–3 and Phase 5–6 MVP_PICKUP items — explicitly out of scope.
- Prometheus / telemetry gaps (G5, G6) — Phase 3.
- Architecture folder commit/approval (G10) — Phase 5.
- Wiring orchestration/MCP into production entry points — Phase 6.
- Full replacement of any eval, nightly, or integration test suite.
- Changing `aria impact --json` behaviour (only `query --json` is in scope).

---

## §2 Shared contracts (verbatim)

### Types / interfaces

| Symbol | Owning subtask | Typed surface | Contract |
|--------|---------------|---------------|----------|
| `placeholder_api_enabled() -> bool` | T1 | `api/config.py:8–10` | Default changed to `"false"` by T1; T2 documents this new default |
| `_success_payload` keys | — | `aria/cli/commands/query.py:47–54` | Frozen key set; T2 does not reference these |

### Naming

No new symbols introduced by T2.

### CLI surface

| Flag / arg | Frozen definition | Notes |
|------------|------------------|-------|
| `aria query <question>` | `aria/cli/commands/query.py:107` | Referenced in live-mode block example if included |
| `aria query --json` | `aria/cli/commands/query.py:127–130` | May be mentioned in Quickstart as optional verification step |

The env var `ARIA_PLACEHOLDER_API` is the only contract-anchor string this subtask introduces into the README. Its spelling must exactly match `api/config.py`.

---

## §4 Subtask spec

**ID:** T2

**Scope:** Add an explicit "Live mode" callout to the README Quickstart (currently lines 50–77) showing the full stack launch sequence with `ARIA_PLACEHOLDER_API=false`. Optionally cross-link or update `§ HTTP Surface — Modes` (currently line 118) to reflect the new default.

**Files to touch:**
1. `README.md` — Quickstart section (approx. lines 50–78) and optionally `§ HTTP Surface — Modes` (approx. line 116–119)

**Contract bindings:** CLI surface (env var spelling `ARIA_PLACEHOLDER_API` must match `api/config.py`); all §2 contracts (no new symbols).

**Inputs:** T1 must have completed and confirmed the G8 flip to `"false"` before T2 executes.

**Outputs:**
- Updated `README.md` with a fenced bash block for live mode inside or directly below the existing Quickstart section
- The block must include `export ARIA_PLACEHOLDER_API=false` (or a `.env` file directive equivalent) and a note that `ARIA_PLACEHOLDER_API=true` restores placeholder/demo mode without backends
- Optional: update `§ HTTP Surface — Modes` line 118 to reflect `false` as new default

**Kill criteria:**
1. Halt if T1 has not completed — specifically, halt if `api/config.py` line 10 still reads `"true"` at execution start. Do not write "the new default is false" in the README if the code flip has not landed.
2. Halt if the env var spelling you are about to write into the README differs from the string `ARIA_PLACEHOLDER_API` as it appears in `api/config.py` line 10.
3. Halt if the Quickstart section has changed substantially from lines 50–77 (the context map's recorded range) — re-read `README.md` lines 45–85 before editing, and if the section structure is unrecognisable, report the difference and await instruction.

**Log tier:** `standard`

**Risks & mitigations:**
- *Risk:* Live-mode block makes placeholder/demo path invisible — mitigate by including a one-sentence note within or after the live-mode block: "To explore without backends, set `ARIA_PLACEHOLDER_API=true`."
- *Risk:* README Quickstart line numbers have shifted since the context map — re-read the file before editing.
- *Risk:* `§ HTTP Surface — Modes` line 118 currently says "default" for placeholder — updating it requires confirming T1 landed; kill criterion (1) covers this.

---

## §5 Filtered adversarial context

### Load-bearing assumptions (this subtask)

*(No §5.2 assumptions were attributed exclusively to T2; the relevant assumption is inherited from T1's completion:)*

If T1 chose document-only instead of flipping the code default, and T2 writes "the default is now false", README will be false. Kill criterion (1) prevents this: read `api/config.py` before writing.

### Hidden couplings (this subtask)

5. `T2 README live-mode block must match the G8 decision outcome | §2 Types row 1 ↔ README.md Quickstart | if T1 chose document-only and T2 writes "default is now false", README is false | T2` — **suspected** (resolved by kill criterion (1))

---

## Resolved inputs

**From T1 (to be supplied at execution time):** Confirmation that `api/config.py` line 10 now reads `os.getenv("ARIA_PLACEHOLDER_API", "false")`. Verify by reading the file before writing any README changes.

**Current README state (for orientation):**

Quickstart is at lines 50–77. Steps 1–6 are a bash block covering clone/install, `.env.example` copy, docker compose, `aria init` + `aria ingest`, `uvicorn api.main:app`, and `pytest`. There is no step that sets `ARIA_PLACEHOLDER_API`. The `§ HTTP Surface — Modes` section (approx. line 116–119) documents placeholder vs live mode but is separated from the Quickstart. The CLI table entry for `aria query` / `aria impact` mentions "placeholder vs live follows `ARIA_PLACEHOLDER_API`" without a live-mode example.

**Suggested live-mode block placement:** Insert after step 5 ("Run the API") or add a `### Live mode` subsection immediately after the closing `` ``` `` of the Quickstart bash block. The subsection should show:
```bash
# Live mode (requires Neo4j + Chroma + LLM configured above)
export ARIA_PLACEHOLDER_API=false   # now the default; shown explicitly for clarity
aria query "Which requirements affect our systems?"
aria impact <REGULATION_ID>
# Placeholder/demo mode (no backends needed): ARIA_PLACEHOLDER_API=true
```

Adjust prose and placement to fit the existing README's style.
