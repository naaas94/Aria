# Executor packet — T1 · G8 flip: code default + config + narrative

**Plan:** mvp-phase4-product-defaults-ux **v1.1** (T1-amend — supersedes v1.0 packet)  
**Log tier:** `architectural`  
**Decision log path:** `.dev/decision-logs/T1-g8-placeholder-default.md` *(create or update as part of outputs)*

---

## Continuation note (post-HALT)

A prior T1 run **halted** on kill criterion (1): `tests/unit/test_metrics.py::TestRetrievalMetrics::test_placeholder_query_does_not_increment` failed after the default flip because the local `client` fixture did not set `ARIA_PLACEHOLDER_API`.

**Partial work may already exist on disk** (`api/config.py`, docs, decision log, CHANGELOG). Do **not** revert. Verify each file in **Files to touch**, apply only missing edits, then land the **`tests/unit/test_metrics.py` fixture fix** and re-run `pytest tests/unit -q`.

---

## §1 Task statement (verbatim)

Phase 4 resolves the long-standing G8 placeholder-default UX gap (open since the architecture folder was drafted) and improves the operator getting-started experience. Concretely: (1) the code default for `ARIA_PLACEHOLDER_API` is flipped from `true` to `false` so operators who skip the README cannot accidentally believe the system works without backends; (2) the README Quickstart gains an explicit "live mode" block showing `ARIA_PLACEHOLDER_API=false` with the full stack; (3) the wet run log template in `.dev/MVP_PICKUP.md` gains explicit `LLM_MODEL=` and `LLM_BASE_URL=` fields so Phase 1 session data capture is unambiguous; and (4) a `aria query --json` CLI smoke test is added to the unit suite to provide contract coverage for the `--json` code path that currently has zero tests.

**Non-goals:**
- Phase 1–3 and Phase 5–6 MVP_PICKUP items — explicitly out of scope.
- Prometheus / telemetry gaps (G5, G6) — Phase 3.
- Architecture folder commit/approval (G10) — Phase 5.
- Wiring orchestration/MCP into production entry points — Phase 6.
- Full replacement of any eval, nightly, or integration test suite.
- Changing `aria impact --json` behaviour (only `query --json` is in scope).

---

## §2 Shared contracts (verbatim)

### Types / interfaces

| Symbol | Owning subtask | Typed surface | Contract | Test |
|--------|---------------|---------------|----------|------|
| `placeholder_api_enabled() -> bool` | T1 | `api/config.py:8–10` | Default env string changes from `"true"` to `"false"`; signature unchanged; truthy set stays `{"1","true","yes"}` | T1 kill criterion: `pytest tests/unit -q` after flip + fixture fix; zero new failures |
| ASGI unit `client` fixture env | T1 | `tests/unit/test_metrics.py` `client` fixture | `monkeypatch.setenv("ARIA_PLACEHOLDER_API", "true")` before `TestClient(app)` so metrics HTTP tests do not depend on live backends or the flipped default | Same run as above; `test_placeholder_query_does_not_increment` must pass |
| `_success_payload(outcome: ComplianceQuerySuccess) -> dict` | — (pre-existing, T4 consumes) | `aria/cli/commands/query.py:47–54` | Keys: `answer: str`, `sources: list`, `retrieval_strategy: str`, `trace: dict`, `aria_mode: Literal["placeholder","live"]` — frozen; T4 may not assert keys outside this set | T4 `test_query_json_placeholder_returns_valid_payload` |

### Error envelope

N/A — no new API routes or error codes introduced.

### Naming

| Symbol | Subtask | Location |
|--------|---------|----------|
| `test_query_json_placeholder_returns_valid_payload` | T4 | `tests/unit/test_cli_entry.py` |
| `T1-g8-placeholder-default.md` | T1 | `.dev/decision-logs/T1-g8-placeholder-default.md` |

### Logging

N/A — no new structured log fields or sinks.

### Tests

- Framework: pytest with `typer.testing.CliRunner` (T4); pytest + `starlette.testclient.TestClient` (T1 amended)
- Location: `tests/unit/test_cli_entry.py` (T4); `tests/unit/test_metrics.py` (T1 amended — `client` fixture only)
- Isolation:
  - **T1 (amended):** `tests/unit/test_metrics.py` `client` fixture must set `ARIA_PLACEHOLDER_API=true` via `monkeypatch` before importing/instantiating `api.main:app` (same pattern as `tests/test_telemetry_endpoints.py`). Fixture-wide, not per-test — keeps all metrics HTTP unit tests backend-independent.
  - **T4:** `runner.invoke(..., env={"ARIA_PLACEHOLDER_API": "true"})` so CLI smoke forces placeholder regardless of G8 default
- Naming prefix: `test_query_json_*` (T4)
- Coverage expectation (T4): exit code 0, stdout parses as JSON, all five `_success_payload` keys present, `aria_mode == "placeholder"`
- *Deferred (documented in CHANGELOG):* no dedicated unit test that `placeholder_api_enabled()` returns `False` when env unset — not required for T1 DoD

### CLI surface

| Flag / arg | Frozen definition | Consuming subtask |
|------------|------------------|------------------|
| `aria query <question>` positional | `aria/cli/commands/query.py:107` | T4 |
| `aria query --json` | `aria/cli/commands/query.py:127–130` | T4 |

### Decision log path (architectural subtask T1)

`.dev/decision-logs/T1-g8-placeholder-default.md` — created by T1. Treat any reference to a different path as a retired-string-sweep target.

---

## §4 Subtask spec

**ID:** T1

**Scope:** Flip `placeholder_api_enabled()` default from `"true"` to `"false"`. Update every surface that documents or relies on the old default. Isolate ASGI unit metrics tests from the flipped default. Write the G8 architectural decision log.

**Files to touch:**
1. `api/config.py` — line 10: change default in `os.getenv("ARIA_PLACEHOLDER_API", …)` from `"true"` to `"false"`
2. `.env.example` — line 44: update comment to document both modes (e.g. `# ARIA_PLACEHOLDER_API=false  # default; set true for placeholder/demo mode without backends`)
3. `api/main.py` — lines 103–105: update OpenAPI description to reflect `false` as default
4. `tests/eval/e2e/test_live_queries.py` — line 8 docstring: reflect new default
5. **`tests/unit/test_metrics.py`** — `client` fixture (~lines 505–512): add `monkeypatch: pytest.MonkeyPatch` parameter; `monkeypatch.setenv("ARIA_PLACEHOLDER_API", "true")` before `from api.main import app` / `TestClient(app)` (mirror `tests/test_telemetry_endpoints.py`)
6. `.dev/MVP_PICKUP.md` — check off G8 row; resolve open-decisions placeholder-default row
7. `.dev/architecture/aria/open-questions.md` — move Q3 to `## Resolved` with date and summary
8. `.dev/decision-logs/T1-g8-placeholder-default.md` — create or update (see guidance below)

**Contract bindings:** `placeholder_api_enabled()` typed surface; ASGI unit `client` fixture env (§2 Tests); Decision log path.

**Inputs:** None.

**Outputs:**
- Updated files 1–8 above
- Decision log includes corrected assumption: `tests/unit/test_metrics.py` mounts ASGI app and required explicit `ARIA_PLACEHOLDER_API=true` after flip
- `pytest tests/unit -q` exits 0

**Kill criteria:**
1. Halt if `pytest tests/unit -q` still has **any failures** after the default flip **and** the `test_metrics.py` fixture fix — report failing test names and stop. (A single failure in `test_placeholder_query_does_not_increment` immediately after flip alone is **expected** until item 5 is applied.)
2. Halt if the current git branch is not `dev` and no explicit user confirmation exists for another target branch.
3. Halt if `.dev/decision-logs/` cannot be created — report and await instruction.
4. Halt if satisfying kill (1) requires edits outside **Files to touch** — report and escalate.

**Log tier:** `architectural`

**Risks & mitigations:**
- *Landed HALT:* `test_metrics.py` `client` fixture — now in scope (item 5).
- *Risk:* Tests outside `tests/unit/` may need env when run manually — T1 does not fix them; nightly/eval set env explicitly.
- *Risk:* `.dev/decision-logs/` blocked — run `git ls-files .dev/` before writing.
- *Risk:* `api/main.py` OpenAPI strings — re-read lines 100–110 before editing.

### Fixture fix reference

```python
@pytest.fixture
def client(monkeypatch: pytest.MonkeyPatch):
    monkeypatch.setenv("ARIA_PLACEHOLDER_API", "true")
    from starlette.testclient import TestClient

    from api.main import app

    with TestClient(app) as c:
        yield c
```

### Decision log content guidance (`.dev/decision-logs/T1-g8-placeholder-default.md`)

Include (architectural tier): Decision, Rationale, Alternatives rejected, **Assumptions** (note ASGI unit-test coupling and fixture fix — do **not** claim "unit tests never read env"), Deferred items (unchanged from plan).

---

## §5 Filtered adversarial context

### Load-bearing assumptions (this subtask)

1. `All existing unit tests pass after flip plus ASGI fixture isolation | api/config.py:10 ↔ tests/unit/test_metrics.py client fixture ↔ §2 Tests (T1) | if additional unit tests mount api.main without env override, kill (1) fires after fixture fix; escalate via kill (4) | T1` — **HALT proved coupling; fixture fix closes it**

3. `.dev/decision-logs/ directory can be created by T1 executor | §2 Decision log path | if directory is .gitignored or blocked, decision log cannot land and kill criterion (3) fires | T1`

### Hidden couplings (this subtask)

1. `placeholder_api_enabled default flip affects any test that instantiates the ASGI app without env override | api/config.py:10 ↔ tests/eval/e2e/test_live_queries.py | live backend connections without ARIA_PLACEHOLDER_API=true | T1` — **confirmed**

3. `api/main.py OpenAPI description hard-codes old default | api/main.py:103 ↔ api/config.py:10 | docs disagree until T1 updates | T1` — **confirmed**

4. `.env.example comment line 44 references old default | .env.example:44 ↔ api/config.py:10 | operator confusion | T1` — **confirmed**

5. `tests/unit/test_metrics.py client fixture mounts api.main without env override | tests/unit/test_metrics.py:505-512 ↔ api/config.py:10 | test_placeholder_query_does_not_increment fails post-flip | T1` — **confirmed (HALT)**

---

## Resolved inputs

No prior subtask inputs.

**Pre-execution check:** Read `api/config.py` 8–10, `api/main.py` 100–110, `.env.example` 40–48, `tests/unit/test_metrics.py` 500–512, `.dev/MVP_PICKUP.md` G8 rows, `.dev/architecture/aria/open-questions.md` Q3, `tests/eval/e2e/test_live_queries.py` 1–12. If flip/docs already landed, skip redundant edits and apply fixture + test run only.
