# Executor packet — T1 · G8 flip: code default + config + narrative

**Plan:** mvp-phase4-product-defaults-ux  
**Log tier:** `architectural`  
**Decision log path:** `.dev/decision-logs/T1-g8-placeholder-default.md` *(create this file as part of outputs)*

---

## §1 Task statement (verbatim)

Phase 4 resolves the long-standing G8 placeholder-default UX gap and improves the operator getting-started experience. Concretely: (1) the code default for `ARIA_PLACEHOLDER_API` is flipped from `true` to `false` so operators who skip the README cannot accidentally believe the system works without backends; (2) the README Quickstart gains an explicit "live mode" block showing `ARIA_PLACEHOLDER_API=false` with the full stack; (3) the wet run log template in `.dev/MVP_PICKUP.md` gains explicit `LLM_MODEL=` and `LLM_BASE_URL=` fields so Phase 1 session data capture is unambiguous; and (4) a `aria query --json` CLI smoke test is added to the unit suite to provide contract coverage for the `--json` code path that currently has zero tests.

**Non-goals:**
- Phase 1–3 and Phase 5–6 MVP_PICKUP items — explicitly out of scope.
- Prometheus / telemetry gaps (G5, G6) — Phase 3.
- Architecture folder commit/approval (G10) — Phase 5.
- Wiring orchestration/MCP into production entry points — Phase 6.
- Full replacement of any eval, nightly, or integration test suite.
- Changing `aria impact --json` behaviour (only `query --json` is in scope).

---

## §2 Shared contracts (verbatim)

### Types / interfaces

| Symbol | Owning subtask | Typed surface | Contract | Test |
|--------|---------------|---------------|----------|------|
| `placeholder_api_enabled() -> bool` | T1 | `api/config.py:8–10` | Default env string changes from `"true"` to `"false"`; signature unchanged; truthy set stays `{"1","true","yes"}` | T1 kill criterion: run `pytest tests/unit -q` after flip; zero new failures required |
| `_success_payload(outcome: ComplianceQuerySuccess) -> dict` | — (pre-existing, T4 consumes) | `aria/cli/commands/query.py:47–54` | Keys: `answer: str`, `sources: list`, `retrieval_strategy: str`, `trace: dict`, `aria_mode: Literal["placeholder","live"]` — frozen; T4 may not assert keys outside this set | T4 `test_query_json_placeholder_returns_valid_payload` |

### Error envelope

N/A — no new API routes or error codes introduced.

### Naming

| Symbol | Subtask | Location |
|--------|---------|----------|
| `test_query_json_placeholder_returns_valid_payload` | T4 | `tests/unit/test_cli_entry.py` |
| `T1-g8-placeholder-default.md` | T1 | `.dev/decision-logs/T1-g8-placeholder-default.md` |

### Logging

N/A — no new structured log fields or sinks.

### Tests

- Framework: pytest with `typer.testing.CliRunner`
- Location: `tests/unit/test_cli_entry.py`
- Isolation: T4 test must pass `env={"ARIA_PLACEHOLDER_API": "true"}` to `runner.invoke(...)` so it forces placeholder mode regardless of the G8-flipped default
- Naming prefix: `test_query_json_*`
- Coverage expectation: exit code 0, stdout parses as JSON, all five `_success_payload` keys present, `aria_mode == "placeholder"`

### CLI surface

| Flag / arg | Frozen definition | Consuming subtask |
|------------|------------------|------------------|
| `aria query <question>` positional | `aria/cli/commands/query.py:107` | T4 |
| `aria query --json` | `aria/cli/commands/query.py:127–130` | T4 |

### Decision log path

`.dev/decision-logs/T1-g8-placeholder-default.md` — T1 creates this file.

---

## §4 Subtask spec

**ID:** T1

**Scope:** Flip `placeholder_api_enabled()` default from `"true"` to `"false"`. Update every surface that documents or relies on the old default. Write the G8 architectural decision log.

**Files to touch:**
1. `api/config.py` — line 10: change `"true"` → `"false"` in `os.getenv("ARIA_PLACEHOLDER_API", "true")`
2. `.env.example` — line 44: update the commented-out line to clearly document both modes (e.g., `# ARIA_PLACEHOLDER_API=false  # default; set true for placeholder/demo mode without backends`)
3. `api/main.py` — lines 103–105: update OpenAPI description; currently reads `"When ARIA_PLACEHOLDER_API=true (default), …"` — change to reflect `false` as the new default
4. `tests/eval/e2e/test_live_queries.py` — line 8 docstring: currently says "By default the app runs in placeholder mode (`ARIA_PLACEHOLDER_API=true`)" — update to reflect new default
5. `.dev/MVP_PICKUP.md` — check off G8 row (line 199); in open-decisions table (line 131), mark the placeholder-default row as resolved (chosen: flip to false on dev)
6. `.dev/architecture/aria/open-questions.md` — move Q3 (placeholder default, currently lines 19–24) to the `## Resolved` section; add resolution date and decision summary
7. `.dev/decision-logs/T1-g8-placeholder-default.md` — create; see decision log content guidance below

**Contract bindings:** `placeholder_api_enabled()` typed surface (§2 row 1); Decision log path (§2 Decision log path section).

**Inputs:** None.

**Outputs:**
- `api/config.py` with default flipped to `"false"`
- `.env.example` with updated comment
- `api/main.py` with updated OpenAPI description
- `tests/eval/e2e/test_live_queries.py` with updated docstring
- `.dev/MVP_PICKUP.md` with G8 checked off and open-decisions row resolved
- `.dev/architecture/aria/open-questions.md` with Q3 moved to Resolved
- `.dev/decision-logs/T1-g8-placeholder-default.md` created (architectural tier)
- `pytest tests/unit -q` exits 0 with zero new failures

**Kill criteria:**
1. Halt if `pytest tests/unit -q` produces **any new failures** after the default flip — report the failing test names and stop. Do not attempt to fix tests outside this packet's Files to touch.
2. Halt if the current git branch is not `dev` and no explicit user confirmation exists that the flip is intended for the actual target branch.
3. Halt if `.dev/decision-logs/` cannot be created (e.g., blocked by `.gitignore`) — report and await instruction.

**Log tier:** `architectural`

**Risks & mitigations:**
- *Risk:* A test outside `tests/unit/` imports the ASGI app and reads the env default without override — kill criterion (1) is scoped to `tests/unit/`; broader failures are reported, not fixed by T1.
- *Risk:* `.dev/decision-logs/` is a new directory — T1 creates it; run `git ls-files .dev/` before writing to confirm no `.gitignore` conflict.
- *Risk:* `api/main.py` OpenAPI description wording is entangled with other strings — re-read lines 100–110 before editing.

### Decision log content guidance (`.dev/decision-logs/T1-g8-placeholder-default.md`)

The decision log must include (architectural tier):

```markdown
# Decision log — G8: placeholder_api_enabled default

**Subtask:** T1 (mvp-phase4-product-defaults-ux)  
**Date:** <execution date>  
**Status:** Landed

## Decision

Flip `ARIA_PLACEHOLDER_API` code default from `"true"` to `"false"` in `api/config.py`.

## Rationale

<why flip on dev: operator UX goal; failing clearly is better than silent synthetic data>

## Alternatives rejected

1. Document-only (keep code default `true`, add README block) — rejected because …
2. Flip only on a release branch — rejected because …

## Assumptions

<list assumptions from §5.2 that are relevant to T1>

## Deferred items

- Nightly CI already sets `ARIA_PLACEHOLDER_API=false` — no change needed.
- `test_live_queries.py` docstring updated (minor); test logic unaffected.
- Architecture `public-interface-inventory.md` lists `placeholder_api_enabled` as "stable" — still stable; default value change does not alter the public signature. G10 (architecture folder commit) is a Phase 5 item.
```

---

## §5 Filtered adversarial context

### Load-bearing assumptions (this subtask)

1. `All existing unit tests pass after the placeholder_api_enabled flip | api/config.py:10 default string ↔ §2 Types row 1 | if any unit test reads ARIA_PLACEHOLDER_API via os.getenv without setting it explicitly, that test breaks and kill criterion (1) fires | T1`

3. `.dev/decision-logs/ directory can be created by T1 executor | §2 Decision log path | if directory is .gitignored or blocked, decision log cannot land and kill criterion (3) fires | T1`

### Hidden couplings (this subtask)

1. `placeholder_api_enabled default flip affects any test that instantiates the ASGI app without env override | api/config.py:10 ↔ tests/eval/e2e/test_live_queries.py (ASGITransport) | after flip, running test_live_queries.py without ARIA_PLACEHOLDER_API=true attempts live backend connections | T1` — **confirmed**

3. `api/main.py OpenAPI description hard-codes "ARIA_PLACEHOLDER_API=true (default)" | api/main.py:103 ↔ api/config.py:10 | after flip, OpenAPI docs disagree with code behavior until updated | T1` — **confirmed**

4. `.env.example comment line 44 references old "true" default | .env.example:44 ↔ api/config.py:10 | operator copies .env.example and gets no explicit value; comments describe old behavior | T1` — **confirmed**

---

## Resolved inputs

No prior subtask inputs. This packet is executable from a clean start.

**Pre-execution check:** Read `api/config.py` lines 8–10, `api/main.py` lines 100–110, `.env.example` lines 40–48, `.dev/MVP_PICKUP.md` lines 127–135 and 197–203, `.dev/architecture/aria/open-questions.md` lines 19–24, and `tests/eval/e2e/test_live_queries.py` lines 1–12 before making any edits to confirm line numbers match expectations from this packet.
