# Executor packet — T3 · Wet run log template + QUICK_TODOS consolidation

**Plan:** mvp-phase4-product-defaults-ux  
**Log tier:** `standard`

---

## §1 Task statement (verbatim)

Phase 4 resolves the long-standing G8 placeholder-default UX gap and improves the operator getting-started experience. Concretely: (1) the code default for `ARIA_PLACEHOLDER_API` is flipped from `true` to `false` so operators who skip the README cannot accidentally believe the system works without backends; (2) the README Quickstart gains an explicit "live mode" block showing `ARIA_PLACEHOLDER_API=false` with the full stack; (3) the wet run log template in `.dev/MVP_PICKUP.md` gains explicit `LLM_MODEL=` and `LLM_BASE_URL=` fields so Phase 1 session data capture is unambiguous; and (4) a `aria query --json` CLI smoke test is added to the unit suite to provide contract coverage for the `--json` code path that currently has zero tests.

**Non-goals:**
- Phase 1–3 and Phase 5–6 MVP_PICKUP items — explicitly out of scope.
- Prometheus / telemetry gaps (G5, G6) — Phase 3.
- Architecture folder commit/approval (G10) — Phase 5.
- Wiring orchestration/MCP into production entry points — Phase 6.
- Full replacement of any eval, nightly, or integration test suite.
- Changing `aria impact --json` behaviour (only `query --json` is in scope).

---

## §2 Shared contracts (verbatim)

### Types / interfaces

No code symbols are touched by T3. All existing interfaces are stable.

### Naming

No new code symbols introduced.

### CLI surface

No CLI flags introduced or modified.

### Partition constraint (binding for T3)

T3 edits **only** the wet run log template block (`.dev/MVP_PICKUP.md` lines 249–268). It does **not** touch:
- Phase 4 checklist rows (lines 197–202) — T1's domain
- G8 open-decisions row (line 131) — T1's domain
- Any other section of MVP_PICKUP.md

Violating this partition risks a merge conflict with T1, which edits lines 131 and 197–202.

---

## §4 Subtask spec

**ID:** T3

**Scope:** Add explicit `LLM_MODEL=` and `LLM_BASE_URL=` fields to the wet run log template in `.dev/MVP_PICKUP.md`. Consolidate the vague `.dev/QUICK_TODOS` qwen note into the template or clear it.

**Files to touch:**
1. `.dev/MVP_PICKUP.md` — wet run log template block (lines 249–268 only)
2. `.dev/QUICK_TODOS` — update or clear the qwen model-swap note

**Contract bindings:** Partition constraint above (§2); env var names must match `.env.example` spelling (`LLM_MODEL`, `LLM_BASE_URL`).

**Inputs:** None — T3 is independent of T1. It may run in parallel with T1.

**Outputs:**
- `.dev/MVP_PICKUP.md` wet run log template with explicit `LLM_MODEL=` and `LLM_BASE_URL=` fields added to the `Environment:` line (or as separate lines)
- `.dev/QUICK_TODOS` updated: either cleared (if the qwen note is fully superseded by the template) or annotated as "consolidated into wet run log template"

**Kill criteria:**
1. Halt if the specific qwen LiteLLM model ID and base URL cannot be resolved to a concrete string. Acceptable resolution sources (in order of preference): (a) the user confirms the model string, (b) `aria/llm/client.py` defaults at lines 180–181, (c) `.env.example` LLM section (lines 13–16). Do **not** write `LLM_MODEL=qwen something` into the template. If no concrete string is available, add the field with the `.env.example` default (`LLM_MODEL=ollama/llama3.2`) and a comment `# Replace with your model, e.g. ollama/qwen2.5 — see LLM_MODEL in .env.example`.
2. Halt if the `## Wet run log` section of MVP_PICKUP.md has already been filled with session data (i.e., it is no longer the blank template) — editing a filled section risks data loss. Report the state and await instruction.
3. Halt if editing the wet run template section would cause a git merge conflict with a concurrent in-progress Phase 1 wet run session fill.

**Log tier:** `standard`

**Risks & mitigations:**
- *Risk:* Flag 5 (qwen model ID unknown) forces halt — fallback per kill criterion (1): use `.env.example` default as the documented example and add a comment directing the operator to replace it for qwen.
- *Risk:* T3 and Phase 1 executor edit the same MVP_PICKUP.md section concurrently — T3 must execute before Phase 1's wet run session begins; if session data is already present, halt per kill criterion (2).
- *Risk:* MVP_PICKUP.md line numbers have shifted — re-read the file before editing. The wet run template starts with the line `## Wet run log (fill on first live session)` and contains the fenced text block with `Date:`, `Environment:`, `ARIA_PLACEHOLDER_API=`, etc.

---

## §5 Filtered adversarial context

### Load-bearing assumptions (this subtask)

4. `Qwen model string is resolvable to a valid LiteLLM model ID before T3 execution | .dev/QUICK_TODOS ↔ .dev/MVP_PICKUP.md wet run template | if the model string cannot be confirmed, kill criterion (1) fires; use fallback approach | T3`

5. `T3 executes before any Phase 1 wet run session fills the MVP_PICKUP.md § Wet run log section | .dev/MVP_PICKUP.md:249-268 ↔ inter-plan sequencing constraint | concurrent fill causes merge conflict and kill criterion (2) fires | T3`

### Hidden couplings (this subtask)

2. `T3 and Phase 1 co-edit MVP_PICKUP.md § Wet run log section | .dev/MVP_PICKUP.md:249-268 ↔ Phase 1 execution artifact | concurrent execution = merge conflict in the same template block | T3` — **confirmed**

---

## Resolved inputs

No prior subtask inputs. This packet is executable immediately.

**Current state of files (for orientation):**

`.dev/QUICK_TODOS` current content (entire file):
```
Local model swap for qwen something - 
```
This is a one-line scratch note. It is the sole source of the qwen intent.

`.dev/MVP_PICKUP.md` wet run log template (lines 249–268):
```text
## Wet run log (fill on first live session)

\`\`\`text
Date:
Environment: (OS, Python, LLM model, Neo4j/Chroma versions)
ARIA_PLACEHOLDER_API=

aria status →
aria init →
aria ingest →
aria query →
aria impact →
aria telemetry →

Failures / surprises:

Fixes applied (commit refs):

Sign-off (MVP golden path OK? Y/N):
\`\`\`
```

**Target state for wet run template (illustrative — adjust style to match file):**

```text
## Wet run log (fill on first live session)

\`\`\`text
Date:
Environment: (OS, Python version, Neo4j version, Chroma version)
LLM_MODEL=          # e.g. ollama/llama3.2 or ollama/qwen2.5 — confirm before run
LLM_BASE_URL=       # e.g. http://localhost:11434
ARIA_PLACEHOLDER_API=false

aria status →
aria init →
aria ingest →
aria query →
aria impact →
aria telemetry →

Failures / surprises:

Fixes applied (commit refs):

Sign-off (MVP golden path OK? Y/N):
\`\`\`
```

Notes:
- The `Environment:` generic line is replaced by specific named fields.
- `ARIA_PLACEHOLDER_API=false` is made explicit (previously a blank `ARIA_PLACEHOLDER_API=`).
- `LLM_MODEL` and `LLM_BASE_URL` field names match `.env.example` exactly.
- If the qwen model string is confirmed before execution, replace the comment with the actual string.

**Env var defaults for reference (from `.env.example` lines 15–16):**
```
LLM_MODEL=ollama/llama3.2
LLM_BASE_URL=http://localhost:11434
```
