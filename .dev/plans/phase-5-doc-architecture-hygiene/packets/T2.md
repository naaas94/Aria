# Executor packet — T2: Add production call graph to `README.md`

**Plan:** phase-5-doc-architecture-hygiene  
**Subtask ID:** T2  
**Log tier:** standard  
**Executor skill:** executor-subtask-execution (read and follow before starting)

---

## §1 Task statement (verbatim from plan)

Phase 5 closes the documentation and architecture hygiene gaps itemized in `.dev/MVP_PICKUP.md` §204–209 (MVP criterion 6: "docs match code"). This subtask addresses item 2:

**README production call graph** — the `## Architecture` section implies the scratch orchestration engine is on the production request path. The actual runtime path (`HTTP/CLI → aria/services → direct clients`) is added as an explicit `**Production call graph:**` note with a prose caveat on the `**Orchestration**` paragraph.

**Non-goals (full plan):**
- Editing path_to_release §2–5
- Adding any automated doc-drift test
- Rewriting the full README `## Architecture` section (ASCII diagram, data-path paragraph, or agents paragraph)
- Moving, renaming, or editing any architecture folder content files
- Any Phase 3 code changes
- Phase 4 or Phase 6 items

**Non-goals (this subtask specifically):**
- Touching the ASCII diagram in `## Architecture`
- Touching the `## Evaluation & Testing`, `## Quickstart`, `## CLI`, or any other README section
- Touching `AUDIT_DIGEST.md`, `path_to_release.md`, or architecture folder files

---

## §2 Shared contracts (verbatim from plan)

| Topic | Binding |
|-------|---------|
| **Types / interfaces** | N/A — doc-only phase. No Python symbols introduced or modified. |
| **Error envelope** | N/A |
| **Naming — supersession date format** | N/A for T2. |
| **Naming — call graph verbatim text** | The production call graph added to README must use the verbatim three-line block from `architectural-patterns.md` lines 76–78 (the fenced block under `## Production call graph (actual runtime)`). Executor may compress to one prose line but must not invent different service names. |
| **Logging** | N/A |
| **Tests** | No executable tests. Kill criteria are verified by direct file inspection (grep-level spot check). No pytest. |
| **CLI surface** | N/A |
| **Decision log paths** | No architectural-tier subtasks in this plan. No decision log required for T2. |

---

## §4 Subtask spec (verbatim from plan)

| Field | Content |
|-------|---------|
| **ID** | T2 |
| **Scope** | (a) Add a `**Production call graph:**` note at the end of the `## Architecture` section body (after the `**Agents**` paragraph, before `## Evaluation & Testing`). Use the verbatim three-line block from `architectural-patterns.md` lines 76–78, formatted as a fenced code block or inline, with a cross-reference to `.dev/architecture/aria/architectural-patterns.md`. (b) Append a parenthetical sentence to the `**Orchestration**` paragraph: "(The scratch engine is fully tested but is not on the production request path today — `POST /query` and `aria query` route through `aria.services` directly.)" |
| **Files to touch** | `README.md` |
| **Contract bindings** | §2 Naming — call graph verbatim text from `architectural-patterns.md`. |
| **Inputs** | None |
| **Outputs** | `README.md` with call-graph addition and orchestration caveat. |
| **Kill criteria** | 1. **Halt** if the `## Architecture` section cannot be precisely located (line boundaries shifted from what was read). Re-read file before editing. 2. **Halt** if the proposed call-graph line would duplicate or contradict the already-present `## CLI (\`aria\`)` section content. 3. **Halt** if context-map flag 4 ambiguity is somehow unresolved — §0 placement decision (end of Architecture section body) is binding. |
| **Log tier** | standard |
| **Risks & mitigations** | Context map Surface 5 (confirmed): call-graph + orchestration paragraph left contradictory without caveat. **Mitigation:** both edits in same subtask, same file. |

---

## Filtered §5.2 load-bearing assumptions (this subtask)

```
(architectural-patterns.md production call graph text is stable at ee870022
 | §2 Naming row: call graph verbatim text from architectural-patterns.md lines 76–78
 | T2 inserts stale or wrong call graph text → README claims contradict patterns file
 | T2)
```

**Verification step (executor must confirm before writing):** Read `.dev/architecture/aria/architectural-patterns.md` and locate the `## Production call graph (actual runtime)` section. Verify the text under it reads (verbatim):

```
HTTP/CLI → aria/services (query, impact)
         → ingestion.pipeline + agents (CLI ingest only)
         → direct Neo4j / Chroma / LLM clients
```

And that the note reads: "Scratch orchestration, MCP, and LangGraph reference are real, tested subsystems but **not connected** to api/main.py or CLI today."

If this text has changed, use the current version from the file — do not rely on this packet's copy.

---

## Filtered §5.4 hidden couplings (this subtask)

```
(README Architecture Orchestration paragraph vs production call graph contradiction
 | README.md ## Architecture **Orchestration** paragraph ↔ T2-added call graph block
 | T2 adds call graph without caveating Orchestration paragraph → two contradictory statements in same section
 | confirmed
 | T2)

(Banner date format inconsistency — latent coupling with T1
 | §2 Naming row: 2026_04_11 (underscores) binding for both T1 and T2
 | A future editor of README writes 2026-04-11 → inconsistency with T1 banner
 | suspected (T2 doesn't cross-link the banner, so coupling is latent not immediate)
 | T1, T2)
```

**Resolution for confirmed coupling (Orchestration paragraph):** Both edits (call graph addition AND orchestration caveat) must land in the same commit/edit session. Do not add the call graph without also appending the caveat to the `**Orchestration**` paragraph.

---

## Resolved inputs

No prior subtask inputs required. T2 is independent.

**Required pre-edit reads (executor must read before writing):**
1. `README.md` — full file to confirm section structure, locate exact insertion points.
2. `.dev/architecture/aria/architectural-patterns.md` — confirm call graph text verbatim (kill criterion + §5.2 assumption).

---

## Implementation guidance

### Orchestration paragraph caveat (edit b — do this first or simultaneously with edit a)

Locate the `**Orchestration**` paragraph in `## Architecture`. It currently reads (approximately):

```
**Orchestration** — A custom stateful graph engine (`aria.orchestration.scratch`) drives an `ARIAState` through named nodes (supervisor, ingestion chain, entity extraction, graph builder, impact analyser). Nodes must return `ARIAState`; invalid returns set `error` instead of crashing. A LangGraph reference implementation exists under `aria.orchestration.langgraph_reference` for comparison.
```

Append to the end of this paragraph (before the closing period or after — whichever reads naturally):

```
(The scratch engine is fully tested but is not on the production request path today — `POST /query` and `aria query` route through `aria.services` directly.)
```

### Production call graph (edit a)

After the `**Agents**` paragraph and before the `## Evaluation & Testing` heading, insert:

```markdown
**Production call graph** — The actual runtime path (per [`.dev/architecture/aria/architectural-patterns.md`](.dev/architecture/aria/architectural-patterns.md)):

```
HTTP/CLI → aria/services (query, impact)
         → ingestion.pipeline + agents (CLI ingest only)
         → direct Neo4j / Chroma / LLM clients
```

Scratch orchestration, MCP, and LangGraph reference are real, tested subsystems but not connected to `api/main.py` or CLI in the current production deployment.
```

*(Adjust markdown formatting as needed for README style consistency — fenced block vs inline. The service names and arrow notation must match verbatim.)*

### Changelog entry (standard tier)

```
[T2] Added production call graph note to README.md ## Architecture section and appended caveat to Orchestration paragraph clarifying that the scratch engine is not on the production request path. Source: architectural-patterns.md Production call graph section.
```

---

## Definition of Done

- [ ] `architectural-patterns.md` call graph text confirmed before editing.
- [ ] `**Production call graph:**` note added at end of `## Architecture` section body (after `**Agents**` paragraph, before `## Evaluation & Testing`).
- [ ] Service names in call graph are verbatim from `architectural-patterns.md`.
- [ ] Cross-reference to `.dev/architecture/aria/architectural-patterns.md` present.
- [ ] `**Orchestration**` paragraph includes caveat that scratch engine is not on production request path.
- [ ] ASCII diagram, `**Data path**` paragraph, `**Agents**` paragraph — **unchanged**.
- [ ] All other README sections — **unchanged**.
- [ ] Standard-tier changelog entry produced.
