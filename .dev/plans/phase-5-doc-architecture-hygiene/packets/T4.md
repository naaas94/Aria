# Executor packet — T4: AUDIT_DIGEST.md open table sync

**Plan:** phase-5-doc-architecture-hygiene  
**Subtask ID:** T4  
**Log tier:** standard  
**Executor skill:** executor-subtask-execution (read and follow before starting)

---

## ⚠ Hard preconditions — check BEFORE reading further

This subtask has two **non-negotiable** execution gates:

1. **`.dev/plans/phase-2-eval-honesty/plan.md` must contain a `Complete` banner.**  
   If not present, **halt immediately** and report: "T4 blocked — Phase 2 plan not yet Complete."

2. **`.dev/plans/phase-3-observability/plan.md` must contain a `Complete` banner.**  
   If not present, **halt immediately** and report: "T4 blocked — Phase 3 plan not yet Complete."

Do not proceed with any AUDIT_DIGEST edits until both conditions pass.

---

## §1 Task statement (verbatim from plan)

Phase 5 closes the documentation and architecture hygiene gaps itemized in `.dev/MVP_PICKUP.md` §204–209 (MVP criterion 6: "docs match code"). This subtask addresses item 4:

**AUDIT_DIGEST.md open table sync** — after Phase 2 (phase-2-eval-honesty) and Phase 3 (phase-3-observability) are Complete, the open/partial table is updated: closed rows move to fixed, new Phase 3 fixed rows added, and row #10 is handled per §0 Flag 5 decision.

**Non-goals (full plan):**
- Editing path_to_release §2–5
- Adding any automated doc-drift test
- Any Phase 3 code changes
- Phase 4 or Phase 6 items

**Non-goals (this subtask specifically):**
- Touching `path_to_release.md`, `README.md`, or architecture folder files
- Moving AUDIT #10 to fixed unless G6 explicitly confirmed landed (§0 Flag 5)
- Adding new audit findings not already in the open/partial table

---

## §2 Shared contracts (verbatim from plan)

| Topic | Binding |
|-------|---------|
| **Types / interfaces** | N/A — doc-only phase. No Python symbols introduced or modified. |
| **Error envelope** | N/A |
| **Naming — supersession date format** | N/A for T4. |
| **Naming — call graph verbatim text** | N/A for T4. |
| **Logging** | N/A |
| **Tests** | No executable tests. Kill criteria are verified by direct file inspection. No pytest. |
| **CLI surface** | N/A |
| **Decision log paths** | No architectural-tier subtasks in this plan. No decision log required for T4. |

**Accuracy constraint (binding for T4):** Every table edit in AUDIT_DIGEST.md must be traceable to a specific artifact: a commit SHA, a CHANGELOG.md entry, or a Phase 2/3 plan §8 completion snapshot. Do not move a row to fixed based on assumption or inference alone.

---

## §4 Subtask spec (verbatim from plan)

| Field | Content |
|-------|---------|
| **ID** | T4 |
| **Scope** | After Phase 2 and Phase 3 are Complete: move rows #2, #8, #18, #19, #20 from the open table to the fixed table if their Phase 2 fix is confirmed. Add new fixed rows for any Phase 3 items (G5/G6/L1/L7) that landed. Update `## Recommended execution order` to remove resolved items. Do NOT move AUDIT #10 to fixed unless `aria/llm/client.py` bare-except sites (lines ~237, ~277) are confirmed absent (per §0 Flag 5). |
| **Files to touch** | `.dev/AUDIT_DIGEST.md` |
| **Contract bindings** | §0 Flag 5 decision: row #10 gate. §0 Flag 2 decision: external Phase 2+3 gate is a hard precondition. |
| **Inputs** | Phase 2 plan Complete state (phase-2-eval-honesty), Phase 3 plan Complete state (phase-3-observability). |
| **Outputs** | Updated `AUDIT_DIGEST.md` with accurate open/fixed tables, removal of resolved rows from Recommended execution order. |
| **Kill criteria** | 1. **Halt if Phase 2 plan (`.dev/plans/phase-2-eval-honesty/plan.md`) does not show a `Complete` banner.** 2. **Halt if Phase 3 plan (`.dev/plans/phase-3-observability/plan.md`) does not show a `Complete` banner.** 3. **Halt** if context-map flag 2 is unresolved — §0 decision (no edits before both plans complete) is binding. 4. **Halt** if any proposed table edit for an item cannot be traced to a specific artifact (commit, CHANGELOG entry, or plan §8) in Phase 2 or Phase 3 output. 5. **Halt** if AUDIT #10 move to fixed is proposed while `aria/llm/client.py` still contains bare `except Exception: pass` at the telemetry write call sites (grep check). |
| **Log tier** | standard |
| **Risks & mitigations** | Context map Surface 3 (confirmed): marking #2 fixed while Phase 2 YAMLs still empty → false green. **Mitigation:** kill criterion 1 blocks execution. Context map Surface 4 (confirmed): #10 vs G6 coupling. **Mitigation:** kill criterion 5 + §0 Flag 5 decision. Phase 3 may complete in stages (G1–G4 before G6). **Mitigation:** executor must confirm G6 specifically before touching #10. |

---

## Filtered §5.2 load-bearing assumptions (this subtask)

```
(Phase 2 plan and Phase 3 plan will eventually reach Complete status
 | T4 kill criteria 1+2
 | T4 never executes if either plan stalls → AUDIT_DIGEST stays stale indefinitely
 | T4)

(AUDIT #10 move to fixed requires G6 landing; G6 ≠ "Phase 3 Complete" unless all Phase 3 goals include G6
 | §0 Flag 5 decision + T4 kill criterion 5
 | T4 prematurely moves #10 if Phase 3 Complete banner does not distinguish partial vs full completion
 | T4)
```

**Executor note on the G6 / #10 assumption:** "Phase 3 Complete" does not automatically imply G6 landed. You must independently verify:
1. Read `aria/llm/client.py` and grep for `except Exception: pass` near lines 237 and 277.
2. If the pattern is absent → G6 landed; #10 may be moved to fixed with citation.
3. If the pattern is still present → do NOT move #10; leave it in open table with a note that it awaits G6.

---

## Filtered §5.4 hidden couplings (this subtask)

```
(AUDIT #10 fixed claim vs aria/llm/client.py bare-except sites
 | AUDIT_DIGEST.md fixed table row #10 ↔ aria/llm/client.py:~237,~277
 | T4 prematurely moves #10 fixed → AUDIT claims what code still does not do
 | confirmed
 | T4)

(path_to_release §4 E1 claim vs Phase 2 outcome after T4 closes AUDIT #2
 | AUDIT_DIGEST open #2 ↔ path_to_release.md §4 E1 ↔ MVP_PICKUP.md G1
 | T4 closes #2 in AUDIT while path_to_release §4 still narrates empty-context failure — T1 banner covers §1 only
 | suspected (T1 scope deliberately excludes §4; residual inconsistency is post-MVP cleanup)
 | T1, T4)
```

**Note on §4 / T4 coupling:** After T4 closes AUDIT #2, `path_to_release.md §4 E1` will still describe the problem as active. This is a known accepted residual inconsistency for this plan version. Do not expand scope to path_to_release §4.

---

## Resolved inputs

**Required pre-edit reads (executor must read before writing):**
1. `.dev/plans/phase-2-eval-honesty/plan.md` — confirm Complete banner (kill criterion 1).
2. `.dev/plans/phase-3-observability/plan.md` — confirm Complete banner (kill criterion 2).
3. `.dev/AUDIT_DIGEST.md` — full file, current state.
4. `CHANGELOG.md` — Phase 2 and Phase 3 entries, to use as citation anchors.
5. `aria/llm/client.py` lines ~220–290 — grep for `except Exception: pass` (kill criterion 5 / G6 check).

---

## Implementation guidance

### Step 1 — Pre-flight checks

```powershell
# Confirm Phase 2 complete
Select-String -Path ".dev/plans/phase-2-eval-honesty/plan.md" -Pattern "Complete"

# Confirm Phase 3 complete
Select-String -Path ".dev/plans/phase-3-observability/plan.md" -Pattern "Complete"

# Check for bare-except in llm/client.py (kill criterion 5)
Select-String -Path "aria/llm/client.py" -Pattern "except Exception: pass"
```

If either plan lacks Complete, or if the bare-except grep returns hits, **halt** per the relevant kill criterion before making any edits.

### Step 2 — Determine which rows to move

For each open/partial row below, check the stated Phase 2 or Phase 3 artifact. Only move rows you can cite explicitly.

| Row | Closes when | Citation source |
|-----|-------------|-----------------|
| **#2** Medium-tier retrieval goldens | Phase 2 closes it (YAML files populated or quarantined) | Phase 2 CHANGELOG or plan §8 |
| **#8** `requires_multi_hop` no-op | Phase 2 closes it (check implemented or flag removed) | Phase 2 CHANGELOG or plan §8 |
| **#9** GraphRAG benchmark (partial) | Phase 2 closes it (methodology disclaimer added) | Phase 2 CHANGELOG or plan §8 |
| **#16** HTTP ingest vs full pipeline (partial) | Docs were already partially fixed; check if Phase 2 fully closes it | Phase 2 plan §8 |
| **#18** CI replay label | Phase 2 closes it (label fixed or fixture added) | Phase 2 CHANGELOG or plan §8 |
| **#19** Nightly Python version gap | Phase 2 closes it (3.13 added to nightly) | Phase 2 CHANGELOG or plan §8 |
| **#20** LangGraph reference | Phase 2 closes it (smoke test or doc note added) | Phase 2 CHANGELOG or plan §8 |
| **#10** Silent except | **Only if G6 confirmed** (see grep check above) | Phase 3 CHANGELOG + aria/llm/client.py code read |

For any row you cannot confirm with a citation, **leave it in the open table** with an updated note (e.g., "Pending Phase 2 artifact confirmation").

### Step 3 — Add new Phase 3 fixed rows (if any)

If Phase 3 addresses new items (G5 Prometheus histogram, G6 bare-except fix, L1 HTTP histogram, L7 ingestion duration), add new rows to the **Fixed** table under a new tier header (e.g., `### Phase 3`) or under an appropriate existing tier. Each new row needs:
- Finding description
- Resolution summary
- CHANGELOG citation

### Step 4 — Update Recommended execution order

Remove rows from `## Recommended execution order` that were moved to fixed. Add note if any rows remain open and why.

### Step 5 — Update date in AUDIT_DIGEST header

Update the `**Date:**` line at the top:
```
**Date:** 2026-04-09 (updated 2026-04-10, synced 2026-05-25 after Phase 2+3)
```

### Changelog entry (standard tier)

```
[T4] Synced AUDIT_DIGEST.md open table after Phase 2 (phase-2-eval-honesty) and Phase 3 (phase-3-observability) Complete. Moved confirmed-fixed rows from open to fixed; updated Recommended execution order. [List which rows moved and which remain open, with reason.]
```

---

## Definition of Done

- [ ] Phase 2 plan Complete banner confirmed before any edit.
- [ ] Phase 3 plan Complete banner confirmed before any edit.
- [ ] `aria/llm/client.py` bare-except check run; result documented.
- [ ] Every moved row has a citation to a Phase 2 or Phase 3 artifact (CHANGELOG entry, commit, or plan §8).
- [ ] AUDIT #10 only moved to fixed if G6 confirmed via grep (no bare-except in client.py).
- [ ] Rows that could not be confirmed remain in open table with updated notes.
- [ ] `## Recommended execution order` updated to reflect remaining open items.
- [ ] AUDIT_DIGEST header date updated.
- [ ] Standard-tier changelog entry produced.
