# Executor packet — T1: Supersession banner on `path_to_release.md` §1

**Plan:** phase-5-doc-architecture-hygiene  
**Subtask ID:** T1  
**Log tier:** standard  
**Executor skill:** executor-subtask-execution (read and follow before starting)

---

## §1 Task statement (verbatim from plan)

Phase 5 closes the documentation and architecture hygiene gaps itemized in `.dev/MVP_PICKUP.md` §204–209 (MVP criterion 6: "docs match code"). This subtask addresses item 1:

**path_to_release.md §1 supersession** — the section `## 1. CLI — does not exist` and the Executive Summary's "No CLI exists" sentence are stale since 2026-04-11. A supersession banner is added; the underlying wet-run risks are preserved.

**Non-goals (full plan):**
- Editing path_to_release §2–5 (observability/eval gaps — Phase 3 owns CHANGELOG for those)
- Adding any automated doc-drift test
- Rewriting the full README `## Architecture` section
- Moving, renaming, or editing any architecture folder content files
- Any Phase 3 code changes
- Phase 4 or Phase 6 items

**Non-goals (this subtask specifically):**
- Touching `## 2`, `## 3`, `## 4`, `## 5`, or `## 6` sections of path_to_release.md
- Deleting or rewriting any existing content below the banner
- Touching README.md, AUDIT_DIGEST.md, or architecture folder files

---

## §2 Shared contracts (verbatim from plan)

| Topic | Binding |
|-------|---------|
| **Types / interfaces** | N/A — doc-only phase. No Python symbols introduced or modified. |
| **Error envelope** | N/A |
| **Naming — supersession date format** | All banner text and cross-references MUST use `2026_04_11` (underscores, matching CHANGELOG.md heading `## 2026_04_11`). The string `2026-04-11` (hyphens) must NOT appear in any new banner or link. Confirmed format from codebase read. |
| **Naming — call graph verbatim text** | N/A for T1. |
| **Logging** | N/A |
| **Tests** | No executable tests. Kill criteria are verified by direct file inspection (grep-level spot check). No pytest. |
| **CLI surface** | N/A |
| **Decision log paths** | No architectural-tier subtasks in this plan. No decision log required for T1. |

---

## §4 Subtask spec (verbatim from plan)

| Field | Content |
|-------|---------|
| **ID** | T1 |
| **Scope** | Add a `> **Superseded (2026_04_11):**` blockquote banner immediately after the `## 1. CLI — does not exist` heading. Add a one-sentence caveat in the `## Executive summary` paragraph striking the "No CLI exists" sentence. All existing content (wet-run risks 1–5, tables, file list) is preserved unchanged below the banner. |
| **Files to touch** | `.dev/path_to_release.md` |
| **Contract bindings** | §2 Naming — supersession date format (`2026_04_11`, underscores). |
| **Inputs** | None |
| **Outputs** | `.dev/path_to_release.md` with banner and exec-summary caveat applied. |
| **Kill criteria** | 1. **Halt** if the CHANGELOG.md heading cannot be confirmed as `## 2026_04_11` before writing the cross-reference (format contract violation). 2. **Halt** if the proposed edit would delete or overwrite any of the five wet-run risk bullets (items 1–5 under "Wet-run risks"). 3. **Halt** if context-map flag 1 is somehow unresolved at execution start — the §0 resolution (banner approach) is binding; do not re-open the fork. |
| **Log tier** | standard |
| **Risks & mitigations** | Context map Surface 7 (suspected): path_to_release §2–3 may also be stale after Phase 3. **Mitigation:** T1 scope is §1 + Executive Summary only; do not touch §2–5. |

---

## Filtered §5.2 load-bearing assumptions (this subtask)

```
(CHANGELOG heading is 2026_04_11 with underscores
 | §2 Naming row: supersession date format
 | T1 writes wrong format (hyphens) → cross-reference points at non-existent heading slug
 | T1)
```

**Verification step (executor must confirm before writing):** Read `CHANGELOG.md` and confirm the heading is `## 2026_04_11`. If the heading uses a different format, **halt** and report — do not proceed with the banner cross-reference.

---

## Filtered §5.4 hidden couplings (this subtask)

```
(Banner date format inconsistency between T1 and any future README cross-link
 | §2 Naming row: 2026_04_11 (underscores) binding for both T1 and T2
 | T1 writes 2026_04_11; a future editor of README writes 2026-04-11 → two inconsistent references
 | suspected (T2 doesn't cross-link the banner, so coupling is latent not immediate)
 | T1, T2)

(path_to_release §4 E1 claim vs Phase 2 outcome after T4 closes AUDIT #2
 | AUDIT_DIGEST open #2 ↔ path_to_release.md §4 E1 ↔ MVP_PICKUP.md G1
 | T4 closes #2 in AUDIT while path_to_release §4 still narrates empty-context failure — T1 banner covers §1 only
 | suspected (T1 scope deliberately excludes §4; residual inconsistency is post-MVP cleanup)
 | T1, T4)
```

**Note on §4 / T1 coupling:** T1 adds a banner to §1 only. §4 E1 inconsistency after Phase 2 lands is a known accepted residual for this plan version. Do not expand scope to §4.

---

## Resolved inputs

No prior subtask inputs required. T1 is independent.

**Required pre-edit reads (executor must read before writing):**
1. `.dev/path_to_release.md` — full file to confirm section structure and locate exact insertion points.
2. `CHANGELOG.md` — confirm heading `## 2026_04_11` (kill criterion 1 prerequisite).

---

## Implementation guidance

### Executive summary edit

Current text (lines 9–12 of path_to_release.md):
```
## Executive summary

ARIA has a solid architectural skeleton: multi-agent orchestration, GraphRAG retrieval, Pydantic contracts, tiered golden-set evaluation, Prometheus + SQLite telemetry, and CI/nightly pipelines. But the system has never been run end-to-end from the terminal as a user would. **No CLI exists.** The API works in placeholder mode but the live path (Neo4j + Chroma + LLM) has not been wet-run through a complete ingest→query→impact flow in one session. The gap between "code compiles and tests pass" and "a human can type a command and watch agents work" is the core blocker.
```

Replace `**No CLI exists.**` with:
```
**~~No CLI exists~~ — see `CHANGELOG.md` `## 2026_04_11`; the `aria` CLI is implemented.**
```

(Or equivalent plain prose: "~~No CLI exists.~~ The `aria` CLI was added in `CHANGELOG.md` `## 2026_04_11`." Use whatever reads most naturally as a struck-through correction.)

### §1 banner

Immediately after the `## 1. CLI — does not exist` heading (line 15), insert:

```markdown
> **Superseded (2026_04_11):** The `aria` CLI is implemented. See `CHANGELOG.md` `## 2026_04_11` for the full change record, and `README.md` `## CLI (\`aria\`)` for current command reference. The wet-run risks below remain informative historical context.
```

Leave all content below the banner (the tables, wet-run risks 1–5, and file list) **unchanged**.

### Changelog entry (standard tier)

Append to the plan's own changelog (executor skill §6 standard tier: what + brief why):

```
[T1] Added supersession banner to path_to_release.md §1 and struck "No CLI exists" from Executive Summary. CLI has been implemented since 2026_04_11; wet-run risks preserved for historical context.
```

---

## Definition of Done

- [ ] `CHANGELOG.md` heading format confirmed as `## 2026_04_11` before any edit.
- [ ] Executive Summary sentence updated (struck or caveated, not deleted).
- [ ] Banner blockquote inserted immediately after `## 1. CLI — does not exist` heading.
- [ ] All content below the banner (wet-run risks 1–5, tables) is byte-for-byte unchanged.
- [ ] No edits to §2, §3, §4, §5, §6 of path_to_release.md.
- [ ] Date format `2026_04_11` (underscores) used throughout — no hyphens.
- [ ] Standard-tier changelog entry produced.
