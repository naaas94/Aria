# Context map — phase-5-doc-architecture-hygiene

**Generated by:** pre-plan-exploration v0.2  
**Commit SHA:** ee87002297a495389b9bc79a510966dd30ab23f7  
**Branch:** dev  
**Working tree:** dirty (`?? .dev/plans/` — exploration artifacts only; in-scope doc targets have no unstaged diff at exploration time)  
**Exploration date:** 2026-05-25  
**Task description:** Phase 5 — Doc & architecture hygiene (MVP_PICKUP.md §204–209 only): rewrite `.dev/path_to_release.md` §1 CLI section OR add banner “Superseded by CHANGELOG 2026-04-11”; sync `.dev/AUDIT_DIGEST.md` open table after Phase 2–3; commit or approve `.dev/architecture/aria/` per user ownership model; add 1-line “production call graph” to README (from `architectural-patterns.md`). Other MVP phases out of scope. Entry hint: `.dev/architecture/aria/INDEX.md`.  
**Repository root:** `c:\Users\Ale\Documents\Repos\Aria`  
**Readiness verdict:** CONDITIONAL  
**Readiness rationale:** `AUDIT_DIGEST` sync is explicitly post–Phase 2–3; `path_to_release` §1 fix is a doc-strategy fork (banner vs rewrite); architecture folder is already git-tracked so “commit” may mean user approval only; README’s existing Architecture prose conflicts with the canonical one-liner unless placement/editing is decided.

---

## §Scope boundary

**Root paths explored:**
- `.dev/path_to_release.md` — stale release-gap doc; §1 falsely claims no CLI (MVP G9)
- `.dev/AUDIT_DIGEST.md` — open/partial audit table to reconcile after Phases 2–3
- `.dev/architecture/aria/INDEX.md` — architecture folder index; commit/approve target (G10)
- `README.md` — production call graph one-liner destination

**Explicit exclusions:**
- `aria/`, `api/`, `tests/` implementation — Phases 2–3 execution (parallel pending plans `phase-2-eval-honesty`, `phase-3-observability`)
- `.github/workflows/` — Phase 2 changes CI labels; Phase 5 only *documents* outcomes in AUDIT_DIGEST
- `tests/eval/golden_set/**` — Phase 2
- `aria/observability/metrics.py`, `aria/llm/client.py`, middleware — Phase 3 (G5/G6); noted only as AUDIT sync inputs
- Phase 4 (`ARIA_PLACEHOLDER_API`, Quickstart live block) — separate pending plan `mvp-phase4-product-defaults-ux`
- Phase 6 MVP+ — out of scope per user
- `.venv/`, `node_modules/`, `eval_runs/`

**File count in scope:** 16 (4 direct doc roots + 10 architecture section files + 2 adjacent reference docs)  
**Visited-set size (Phase 1 traversal):** 14 (unique paths in forward depth-2 link/read + reverse grep from roots)

**Overlapping concurrent exploration:** None for Phase 5. Pending maps `phase-2-eval-honesty` and `phase-3-observability` explicitly exclude Phase 5 docs; no merge HALT required. Phase 5 AUDIT sync *depends on* 2–3 landing but does not duplicate their file maps.

---

## §File map

| path | role | in_task_scope | rationale |
|------|------|---------------|-----------|
| `.dev/path_to_release.md` | Release gap analysis (2026-04-10); §1 CLI stale | direct | Rewrite §1 or supersession banner per task |
| `.dev/AUDIT_DIGEST.md` | Validated audit open/fixed tables | direct | Sync open table after Phase 2–3 |
| `.dev/architecture/aria/INDEX.md` | Architecture folder manifest + versions | direct | Commit/approve gate; skill consumption entry |
| `.dev/architecture/aria/architectural-patterns.md` | Patterns + **Production call graph** source | direct | README one-liner source; G7 reference |
| `.dev/architecture/aria/module-map.md` | Module ownership map | direct | Part of folder approval bundle |
| `.dev/architecture/aria/public-interface-inventory.md` | Cross-boundary symbol inventory | direct | Orchestrator/auditor SSOT |
| `.dev/architecture/aria/data-contract-registry.md` | Pydantic contract registry | direct | Same |
| `.dev/architecture/aria/dependency-graph.md` | Import/production path edges | direct | Same |
| `.dev/architecture/aria/integration-seams.md` | External I/O seams | direct | Same |
| `.dev/architecture/aria/external-input-sources.md` | Untrusted input catalog | direct | same |
| `.dev/architecture/aria/failure-taxonomy.md` | Failure-mode vocabulary | direct | Same |
| `.dev/architecture/aria/known-coupling-surfaces.md` | Hidden coupling catalog | direct | Same |
| `.dev/architecture/aria/open-questions.md` | Unresolved architecture questions | direct | May need INDEX cross-link after MVP decisions |
| `.dev/architecture/aria/changelog.md` | Architecture folder version log | direct | Updated on folder approval |
| `README.md` | Primary operator/onboarding doc | direct | Add 1-line production call graph |
| `CHANGELOG.md` | Implementation changelog (`## 2026_04_11` CLI) | adjacent | Supersession anchor for path_to_release banner |
| `.dev/MVP_PICKUP.md` | Living MVP backlog (G9, G10, Phase 5 checklist) | adjacent | Defines tasks and stale-doc narrative |
| `pyproject.toml` | `[project.scripts] aria = aria.cli.main:main` | adjacent | Ground truth contradicting path_to_release §1 |
| `.dev/telemetry-audit.md` | Telemetry audit narrative | adjacent | path_to_release L1 cites path labels; not edited in Phase 5 unless AUDIT cross-ref added |

---

## §Interface inventory

*Markdown/doc artifacts: “symbols” are stable sections or tables, not Python exports.*

### `.dev/path_to_release.md`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `## Executive summary` | section | Lines 9–12: claims no CLI, wet-run blocker | `.dev/MVP_PICKUP.md` (G9 narrative) | suspect_modified | none_found |
| `## 1. CLI — does not exist` | section | Lines 15–61: missing CLI table, files-to-create list | `.dev/MVP_PICKUP.md`, `.dev/AUDIT_DIGEST.md` (indirect via eval/CLI themes) | suspect_modified | none_found |
| `## 2. Cost tracking` | section | Gap table C1–C6 including C5 LLM swallow | `phase-3-observability` context-map (C5/G6) | unknown | none_found |
| `## 3. Latency / health` | section | Gaps L1–L8 including L1/L6/L7 | Phase 3 plan | unknown | none_found |
| `## 4. Eval polish` | section | E1 retrieval goldens (#2) | Phase 2 plan, AUDIT #2 | unknown | none_found |
| `## 9. Execution plan` | section | Checklist rows for metrics/CLI | adjacent planning only | stable | none_found |

### `.dev/AUDIT_DIGEST.md`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `## Open and partial items` | table | Rows #2, #8, #9 partial, #16 partial, #18, #19, #20 | `.dev/MVP_PICKUP.md` repo map; Phase 2–3 executors | suspect_modified | none_found |
| `## Recommended execution order` | table | P0 #2, P1 #8, P2 #18/#19/#20 | MVP pickup priorities | unknown | none_found |
| `## Fixed (resolved)` | table | P0 #1, P1 #3–7, P2 #10–17, P3 #22–30 | CHANGELOG.md cross-ref in intro | stable | none_found |

### `README.md`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `## Architecture` | section | ASCII diagram + Data path + Orchestration + Agents (lines 7–30) | Onboarding; implies orchestrator on query path | suspect_modified | none_found |
| `## CLI (\`aria\`)` | section | Subcommand list (lines 81+) | CHANGELOG 2026_04_11; aligns with implemented CLI | stable | `tests/unit/test_cli_entry.py` |
| `## Quickstart` | section | `aria init` / `aria ingest` flow | CHANGELOG; path_to_release wet-run risks | unknown | none_found |

### `.dev/architecture/aria/architectural-patterns.md`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `## Production call graph (actual runtime)` | section | 3-line code block: `HTTP/CLI → aria/services …` + scratch/MCP not connected note | MVP G7; README one-liner source | suspect_modified | none_found |
| Pattern blocks (`contracts-first`, `shared-services`, …) | section | Fenced pattern + falsifier each | project-architecture / auditor-review consumers | stable | various unit/eval tests per falsifier notes |

### `.dev/architecture/aria/INDEX.md`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| Metadata header | constant | `Document version: 1.1.0`, `Stale after: 2026-06-23` | pre-plan / orchestrator staleness checks | stable | none_found |
| `Files:` table | constant | Lists 10 section files + versions | Skill consumers per INDEX | stable | none_found |

---

## §Prior reasoning

No `.dev/decision-logs/` directory found.

| log path | subtask | landed decision (1 line) | open deferrals (1 line) |
|----------|---------|--------------------------|------------------------|
| — | — | — | — |

**Substitute sources:**
- `CHANGELOG.md` `## 2026_04_11` — CLI, `aria.services`, health/readiness, README CLI section (canonical over path_to_release §1).
- `.dev/architecture/aria/changelog.md` — Initial construction 2026-05-24; patterns/coupling/open-questions bumps same day.

---

## §Coupling surfaces

**Grep patterns checked:**
- `path_to_release`, `AUDIT_DIGEST`, `architecture/aria`, `.dev/architecture`
- `Superseded`, `2026-04-11`, `2026_04_11`, `CHANGELOG`
- `Production call graph`, `HTTP/CLI → aria/services`
- Audit IDs `#2`, `#8`, `#18`, `#10`, `C5`, `L1`, `G6`, `G5`
- `retrieved_context`, `includes replay`, `requires_multi_hop`
- `aria = "aria.cli.main:main"`, `CLI — does not exist`, `no CLI`

**Surface 1** · confirmed
```
(Stale “no CLI” narrative | .dev/path_to_release.md:## 1 + Executive summary | Operators follow path_to_release and skip existing `aria` commands; duplicate work on cli/ package | path_to_release hygiene)
```

**Surface 2** · confirmed
```
(Supersession date string format | MVP_PICKUP banner “CHANGELOG 2026-04-11” vs CHANGELOG heading “## 2026_04_11” | Broken or ambiguous link if banner uses wrong slug; search/git log mismatch | path_to_release banner option)
```

**Surface 3** · confirmed
```
(Audit item #2 retrieval goldens | .dev/AUDIT_DIGEST.md open #2 ↔ .dev/path_to_release.md §4 E1 ↔ .dev/MVP_PICKUP.md G1 | Phase 5 marks fixed in AUDIT while Phase 2 YAMLs still empty → false “green” audit story | AUDIT sync, Phase 2)
```

**Surface 4** · confirmed
```
(Audit #10 “silent except” fixed vs LLM client swallow | AUDIT_DIGEST fixed #10 (middleware/agents) ↔ path_to_release C5/B1 ↔ aria/llm/client.py:237,277 still `except Exception: pass` | Phase 5 moves #10 scope to fixed without Phase 3 G6 → digest contradicts code | AUDIT sync, Phase 3)
```
*Disproven by:* Phase 3 landing G6 before AUDIT sync.

**Surface 5** · confirmed
```
(README architecture story vs production call graph | README.md:## Architecture orchestration prose ↔ architectural-patterns.md:Production call graph | One-liner added without editing § Architecture leaves contradictory onboarding | README call graph)
```

**Surface 6** · confirmed
```
(Relative link paths in MVP_PICKUP | MVP links `architecture/aria/INDEX.md` without `.dev/` prefix | Resolves incorrectly from repo root vs `.dev/` tree | doc hygiene, architecture approve)
```

**Surface 7** · suspected
```
(path_to_release observability gaps vs post–Phase 3 code | path_to_release L1/L6/L7/C1 still open ↔ metrics.py may gain histograms in Phase 3 | path_to_release §2–3 remain stale after Phase 3 if only AUDIT synced | path_to_release beyond §1)
```
*Confirm by:* reading `aria/observability/metrics.py` after Phase 3 merge; Phase 5 scope is §1 OR banner only unless orchestrator expands.

**Surface 8** · confirmed
```
(Architecture folder ownership | project-architecture skill: user approves before agents treat as SSOT; agents flag drift only | Premature “committed” without user review → orchestrator plans against unapproved assumptions | architecture commit/approve)
```

---

## §Ambiguity flags

**Flag 1** · category: ownership
```
(For path_to_release §1, banner only or full §1 rewrite pointing at aria/cli + CHANGELOG? | Task is OR; §1 is 47 lines of wrong “files to create”; banner is 1–3 lines | Banner leaves Executive summary “no CLI” stale; rewrite costs more but satisfies MVP “docs match code” | path_to_release §1)
```

**Flag 2** · category: ownership
```
(When may AUDIT_DIGEST open table be edited — only after Phase 2 and 3 merge, or incremental doc notes now? | MVP_PICKUP §207 explicit sequencing | Updating #2/#8/#18 before Phase 2 lands misstates CI; deferring entire Phase 5 item blocks “docs match code” if 2–3 slip | AUDIT sync)
```

**Flag 3** · category: ownership
```
(What does “commit or approve architecture/aria/” require given git already tracks all 11 files? | git ls-files shows tracked paths; MVP G10 said untracked at session start (stale) | Empty commit or approval-only checklist with no user sign-off leaves G10 open forever | architecture folder)
```

**Flag 4** · category: vocabulary_collision
```
(README “production call graph” placement — new line under ## Architecture vs replace orchestration paragraph vs footnote to .dev/architecture?) | Task says “1-line”; README § Architecture is multi-paragraph aspirational diagram | One line without surrounding caveat perpetuates orchestrator-on-FastAPI misconception | README)
```

**Flag 5** · category: coexisting_model_versions
```
(AUDIT #10 resolution scope vs path_to_release C5 / G6 | Fixed table says telemetry swallow fixed; client.py still passes on record_llm_call failures | Phase 5 sync lists #10 as complete while G6 open → operator trust loss | AUDIT sync + Phase 3)
```

**Flag 6** · category: missing_test_coverage
```
(Doc-only phase — no automated test for doc drift) | No test asserts README contains production call graph or path_to_release lacks “no CLI” | Regressions only caught by manual review or future doc lint | doc hygiene)
```
*Note:* Not blocking planning; auditor may want a lightweight grep-based check in CI later (out of Phase 5 scope unless user expands).

---

## §Orchestrator handoff notes

- **Ground truth for CLI (2026-05-25):** `pyproject.toml` has `aria = "aria.cli.main:main"`; `CHANGELOG.md` `## 2026_04_11` documents `aria/cli/*`, `aria.services`, health probes; README Quickstart uses `aria init` / `aria ingest`. `.dev/path_to_release.md` §1 and executive summary still state CLI does not exist.
- **AUDIT open rows unchanged at exploration:** #2, #8, #9 partial, #16 partial, #18, #19, #20 — align with Phase 2 checklist. #19 (nightly 3.13) is Phase 2/doc-only. No AUDIT row for G5/G6/L1/L6/L7; Phase 3 completion may require *new* fixed rows or path_to_release edits, not only open-table moves.
- **Phase 3 code state (read-only check):** `aria/llm/client.py` still has `except Exception: pass` at telemetry write sites (~237, ~277). Prometheus histograms from G5 not present in codebase grep (planned in `phase-3-observability` map).
- **Architecture folder:** All 11 files tracked on `dev` at `ee870022`; no diff vs HEAD for those paths. “Commit” task likely means **user approval** per project-architecture ownership model, then optionally a no-op or INDEX/changelog acknowledgment commit.
- **Canonical one-liner (verbatim source for README):** From `architectural-patterns.md` lines 76–78: `HTTP/CLI → aria/services (query, impact)` → ingestion/agents (CLI ingest) → direct Neo4j/Chroma/LLM (executor may compress to single README line per task).
- **Implicit contract:** MVP pickup and architecture docs use relative links `architecture/aria/` under `.dev/` in prose but path `architecture/aria/` without `.dev` in some link targets — consistent broken-link risk across `.dev/*.md`.
- **Working tree:** Only `?? .dev/plans/` dirty; not in-scope application code.
