# Context map — mvp-phase4-product-defaults-ux

**HALT (overlap):** Pending exploration [mvp-phase1-golden-wet-run](mvp-phase1-golden-wet-run/context-map.md) shares `api/config.py`, `README.md`, `.env.example`, and `.dev/MVP_PICKUP.md` § Wet run log. User scoped **Phase 4 only** (MVP_PICKUP.md lines 197–203); orchestrator should **partition** (Phase 4: G8 default decision, README Quickstart live block, QUICK_TODOS → wet-run LLM fields, optional CLI smoke) or **supersede** Phase 1 map entries for those files before parallel execution.

**Generated by:** pre-plan-exploration v0.2  
**Commit SHA:** ee87002297a495389b9bc79a510966dd30ab23f7  
**Branch:** dev  
**Working tree:** dirty (`?? .dev/plans/`)  
**Exploration date:** 2026-05-25  
**Task description:** Phase 4 — Product defaults & operator UX (0.5 day): (1) Decide placeholder default for “MVP branch” (G8); (2) README Quickstart: explicit “live mode” block with `ARIA_PLACEHOLDER_API=false`; (3) QUICK_TODOS: Local model swap (qwen) — document `LLM_MODEL` / `LLM_BASE_URL` in wet run log; (4) Optional: `aria query --json` smoke in CI via CliRunner (help tests already exist). Other MVP_PICKUP phases out of scope.  
**Repository root:** `c:\Users\Ale\Documents\Repos\Aria`  
**Readiness verdict:** CONDITIONAL  
**Readiness rationale:** G8 is an unresolved product decision (flip default vs document-only); wet-run log LLM field ownership overlaps Phase 1 until partitioned.

---

## §Scope boundary

**Root paths explored:**
- `api/config.py` — `placeholder_api_enabled()` default string (`"true"`) is the G8 code surface.
- `README.md` — Quickstart (lines 50–77) and HTTP Modes (lines 116–119) lack a dedicated live-mode callout block.
- `.dev/MVP_PICKUP.md` — G8 row, open architectural decisions (line 131), Phase 4 checklist, § Wet run log template (lines 249–268).
- `.dev/QUICK_TODOS` — one-line qwen model-swap note; target for consolidation into wet run log.
- `tests/unit/test_cli_entry.py` — existing CliRunner help smoke; extension point for optional `aria query --json` smoke.
- `.github/workflows/ci.yml` — `tests/unit` step already runs all unit tests (would pick up new CLI smoke without workflow edit if test added).

**Explicit exclusions:**
- MVP_PICKUP Phases 0–3, 5–6 — user-declared out of scope.
- `tests/eval/golden_set/`, retrieval goldens, Prometheus/telemetry code — Phases 2–3.
- `aria/orchestration/`, MCP/A2A production wiring — Phase 6 / out of MVP.
- Full CLI ingest/init/status/serve implementation paths — not required for Phase 4 doc/default work (only `query --json` smoke if optional item is taken).
- `node_modules/`, `.venv/`, `dist/` — convention.

**File count in scope:** 18  
**Visited-set size (Phase 1 traversal):** 22 (forward depth-2 from roots + reverse depth-1 callers of `api/config.py`)

---

## §File map

| path | role | in_task_scope | rationale |
|------|------|---------------|-----------|
| `api/config.py` | `placeholder_api_enabled()` env gate | direct | G8: single source of default `ARIA_PLACEHOLDER_API=true`. |
| `README.md` | Operator docs, Quickstart | direct | Task requires explicit live-mode block in Quickstart. |
| `.dev/MVP_PICKUP.md` | MVP backlog + wet run log template | direct | G8 decision context; wet run log sink for `LLM_MODEL` / `LLM_BASE_URL`. |
| `.dev/QUICK_TODOS` | Scratch operator note (qwen) | direct | Source text for model-swap documentation task. |
| `tests/unit/test_cli_entry.py` | CliRunner help smoke | direct | Optional `aria query --json` smoke extension. |
| `.github/workflows/ci.yml` | CI pytest steps | direct | Optional smoke lands under existing `tests/unit` step. |
| `.env.example` | Env template | direct | Documents `ARIA_PLACEHOLDER_API`, `LLM_MODEL`, `LLM_BASE_URL`; must stay aligned if G8 flips default. |
| `aria/cli/commands/query.py` | `aria query`, `--json` | transitive | Imports `placeholder_api_enabled`; smoke target. |
| `aria/cli/commands/impact.py` | `aria impact` | transitive | Same placeholder gate as query (G8 affects both). |
| `aria/cli/main.py` | Typer app, `load_dotenv` | transitive | CliRunner invokes `app`; root loads `.env` before subcommands. |
| `api/routers/query.py` | `POST /query` | transitive | HTTP mirror of placeholder flag. |
| `api/routers/impact.py` | `GET /impact` | transitive | HTTP mirror of placeholder flag. |
| `api/main.py` | FastAPI app metadata | transitive | OpenAPI description mentions placeholder default. |
| `aria/services/compliance_query.py` | `run_compliance_query` placeholder branch | transitive | Defines deterministic placeholder answer text/JSON fields for CLI smoke assertions. |
| `aria/services/impact_report.py` | `run_impact_report` placeholder branch | transitive | Same flag; impact not in optional smoke but G8/docs affect it. |
| `aria/llm/client.py` | `LLMClient` env defaults | adjacent | `LLM_MODEL` / `LLM_BASE_URL` defaults for wet-run documentation. |
| `aria/health/assessment.py` | `probe_llm_reachable` env reads | adjacent | Same env keys as client; wet-run `aria status` / ingest preflight. |
| `.dev/architecture/aria/open-questions.md` | Architecture open question #3 | adjacent | Records placeholder-default question; should close when G8 resolves. |
| `tests/eval/e2e/test_live_queries.py` | Live E2E (nightly) | adjacent | Documents default placeholder mode; flip-default would change operator expectations. |

---

## §Interface inventory

### `api/config.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `placeholder_api_enabled` | function | `() -> bool`; reads `ARIA_PLACEHOLDER_API`, default `"true"` | `api/routers/query.py`, `api/routers/impact.py`, `aria/cli/commands/query.py`, `aria/cli/commands/impact.py` | suspect_modified | none_found |
| `strict_schema_version_expected` | function | `() -> str \| None` | (out of Phase 4 task) | stable | — |
| `cors_allow_origins` | function | `() -> list[str]` | (out of Phase 4 task) | stable | — |
| `is_production_deployment` | function | `() -> bool` | (out of Phase 4 task) | stable | — |
| `telemetry_retention_days` | function | `() -> int \| None` | (out of Phase 4 task) | stable | — |
| `telemetry_prune_interval_seconds` | function | `() -> int` | (out of Phase 4 task) | stable | — |
| `observability_public_while_api_key_configured` | function | `() -> bool` | (out of Phase 4 task) | stable | — |

### `aria/cli/commands/query.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `query` | function | `question: str`, `regulation_id: str \| None = None`, `use_graph_rag: bool = True`, `top_k: int = 10`, `as_json: bool = False` → `None` (raises `typer.Exit`) | `aria/cli/main.py` (`app.command("query")`) | unknown | `tests/unit/test_cli_entry.py` (help only) |
| `_query_async` | function | `*, question, regulation_id, use_graph_rag, top_k, as_json` → `int` | `query` | unknown | none_found |
| `_success_payload` | function | `outcome: ComplianceQuerySuccess` → `dict[str, Any]` | `_query_async` when `as_json` | unknown | none_found |
| `_print_human_success` | function | `outcome: ComplianceQuerySuccess` → `None` | `_query_async` | stable | none_found |

### `aria/services/compliance_query.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `ComplianceQueryRequest` | pydantic_model | `question: str`, `regulation_id: str \| None`, `use_graph_rag: bool`, `top_k: int` | CLI, API routers | stable | (service tests elsewhere) |
| `ComplianceQuerySuccess` | pydantic_model | `response: ComplianceQueryResponse`, `aria_mode: Literal["placeholder","live"]` | CLI, API | stable | none_found for placeholder path |
| `run_compliance_query` | function | `request_dto, conns, *, use_placeholder: bool` → `ComplianceQueryOutcome` | `aria/cli/commands/query.py`, `api/routers/query.py` | unknown | none_found |

Placeholder branch returns answer prefix `[Placeholder] Would answer:` and `sources=[]`, `aria_mode="placeholder"`.

### `tests/unit/test_cli_entry.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `test_root_help_exits_zero` | function | `() -> None` | pytest | suspect_modified | self |
| `test_query_help_lists_options` | function | `() -> None` | pytest | suspect_modified | self |

No test invokes `query` with arguments or `--json`.

### `README.md` (sections in scope)

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| § Quickstart | doc_section | bash blocks lines 52–77 | operators | suspect_modified | none_found |
| § HTTP Surface — Modes | doc_section | `ARIA_PLACEHOLDER_API` lines 116–119 | operators | suspect_modified | none_found |
| § CLI table | doc_section | mentions placeholder follows env | operators | unknown | none_found |

Quickstart steps 1–6 do not set `ARIA_PLACEHOLDER_API=false` or label a “live mode” subsection.

### `.dev/MVP_PICKUP.md` (sections in scope)

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| Phase 4 checklist | doc_section | lines 197–202 | owner | suspect_modified | none_found |
| § Wet run log | doc_section | template lines 251–268 | Phase 1 + Phase 4 | suspect_modified | none_found |
| G8 row / open decisions | doc_section | lines 94, 131 | G8 resolution | suspect_modified | none_found |

Wet run template has `Environment: (OS, Python, LLM model, …)` but no explicit `LLM_MODEL=` / `LLM_BASE_URL=` fields.

---

## §Prior reasoning

No files under `.dev/decision-logs/` (directory empty or absent). Landed decisions for placeholder mode live in:

| log path | subtask | landed decision (1 line) | open deferrals (1 line) |
|----------|---------|--------------------------|------------------------|
| `.dev/architecture/aria/open-questions.md` | — | Question: production target for placeholder mode — default off vs default-on for portfolio demos | Closes when: default flipped or environments explicitly documented |
| `.dev/MVP_PICKUP.md` § Open architectural decisions | — | Placeholder default? **Flip default to false for “release” branch** OR document `ARIA_PLACEHOLDER_API=false` in MVP checklist prominently | Not yet checked off in Phase 4 |

---

## §Coupling surfaces

**Grep patterns checked:**
- `ARIA_PLACEHOLDER_API`, `placeholder_api_enabled`
- `LLM_MODEL`, `LLM_BASE_URL`
- `--json` (Typer CLI)
- `CliRunner`, `test_cli_entry`
- `Wet run log`, `QUICK_TODOS`
- `.dev/decision-logs/`
- `aria_mode`, `[Placeholder]`

**Surface 1** · confirmed
```
(ARIA_PLACEHOLDER_API default "true" | api/config.py:placeholder_api_enabled | CLI/API return synthetic query/impact with empty sources; operators believe MVP works without Neo4j/Chroma/LLM | G8 default decision, README live block, CLI smoke in placeholder mode)
```

**Surface 2** · confirmed
```
(placeholder flag read at call time, not cached | api/config.py:placeholder_api_enabled ↔ aria/cli/commands/query.py:71 ↔ api/routers/query.py:48 | changing default in .env.example without code default mismatch confuses readers; runtime follows getenv default in code | G8, .env.example, README)
```

**Surface 3** · confirmed
```
(LLM_MODEL / LLM_BASE_URL duplicated defaults | aria/llm/client.py:180-181 ↔ aria/health/assessment.py:88-89 ↔ .env.example:15-16 | wet run log documents one pair but status/ingest/client may diverge if only .env partially updated | QUICK_TODOS qwen doc, wet run log)
```

**Surface 4** · confirmed
```
(CliRunner smoke inherits process env | tests/unit/test_cli_entry.py ↔ aria/cli/main.py:_root load_dotenv | optional `aria query --json` smoke fails in CI if env sets ARIA_PLACEHOLDER_API=false without backends; passes only with explicit monkeypatch/unset | optional CI smoke)
```

**Surface 5** · confirmed
```
(Wet run log template in MVP_PICKUP | .dev/MVP_PICKUP.md:249-268 ↔ Phase 1 execution artifact | Phase 4 adds LLM_MODEL/LLM_BASE_URL lines while Phase 1 fills session results; merge conflicts in same section | wet run documentation)
```

**Surface 6** · suspected
```
("MVP branch" meaning | .dev/MVP_PICKUP.md:131 "release branch" vs current git branch `dev` | G8 flip applied on wrong branch or only documented on dev | G8 decision)
```
Disproven by: explicit user/orchestrator note naming target git branch (e.g. `main`, `release/mvp`).

**none_found_by_pattern:** shared decision-log path strings under `.dev/decision-logs/` (no logs present).

---

## §Ambiguity flags

**Flag 1** · category: ownership (product decision)
```
(Should G8 flip the code default to false, or only document ARIA_PLACEHOLDER_API=false in README/MVP checklist? | api/config.py:10 defaults "true"; MVP_PICKUP.md:131 lists both options unchecked; architecture open-questions.md #3 unresolved | flip breaks "safe exploration" default and may affect e2e expecting placeholder unless tests monkeypatch; document-only leaves footgun for operators who skip README | G8, README, .env.example)
```

**Flag 2** · category: vocabulary_collision
```
(What is "MVP branch" for the G8 decision? | Task text "MVP branch"; MVP_PICKUP suggests "release branch"; repo on `dev` | default flip committed to wrong branch or never promoted to release | G8)
```

**Flag 3** · category: ownership
```
(Where should qwen LLM_MODEL / LLM_BASE_URL be recorded — QUICK_TODOS only, wet run log template, .env.example comment, or all three? | QUICK_TODOS is a single vague line; wet run log has generic "LLM model" only; .env.example shows ollama/llama3.2 | operator copies wrong model id; ingest preflight probes wrong endpoint | QUICK_TODOS, wet run log)
```

**Flag 4** · category: missing_test_coverage
```
(What should optional `aria query --json` smoke assert? | Placeholder JSON keys: answer, sources, retrieval_strategy, trace, aria_mode (via _success_payload); no existing contract test | brittle test if placeholder answer string changes; too weak if only exit code 0 | optional CI smoke)
```

**Flag 5** · category: coexisting_model_versions
```
(Which qwen LiteLLM model id and base URL for local swap? | QUICK_TODOS: "qwen something"; .env.example: ollama/llama3.2 + http://localhost:11434 | wet run documents non-working model string | QUICK_TODOS / wet run log)
```

**none_found_by_category:** deprecation_marker_absent — `placeholder_api_enabled` is actively consumed; not deprecated.

---

## §Orchestrator handoff notes

- **Architecture reference:** [`.dev/architecture/aria/`](../../architecture/aria/INDEX.md) — `known-coupling-surfaces.md` already documents env sprawl including `ARIA_PLACEHOLDER_API default true` (confirmed). `public-interface-inventory.md` lists `placeholder_api_enabled` as **stable**; G8 change would contradict that entry until architecture folder is updated (Phase 5 item G10).
- **README Quickstart gap:** Live stack steps exist (docker, `aria init`, `aria ingest`) but no fenced block exporting `ARIA_PLACEHOLDER_API=false` before query/impact; HTTP § Modes is separate from Quickstart.
- **CI:** `ci.yml` line 53 runs `pytest tests/unit`; new test in `test_cli_entry.py` needs no workflow change. Nightly sets `ARIA_PLACEHOLDER_API: "false"` (`.github/workflows/nightly.yml`) — unrelated to optional unit smoke.
- **Implicit contract:** `aria` callback always `load_dotenv()` — live-mode docs should tell operators to set vars in repo-root `.env`, not only inline exports.
- **Phase 1 overlap:** Do not re-execute Phase 1 wet run in Phase 4 plan unless partitioned; Phase 4 can still **edit** wet run log **template** (add `LLM_MODEL` / `LLM_BASE_URL` lines) before Phase 1 fills session data.
- **Working tree:** Only `?? .dev/plans/` dirty; no in-scope source files modified at exploration time.
