id: security-a2a-secret
category: security
tier: fast
tags: [security, a2a, auth]
description: >
  When A2A_SHARED_SECRET is set, /a2a/tasks requires X-A2A-Secret header.
input: {}
expect:
  security:
    check_type: a2a_secret_enforcement
